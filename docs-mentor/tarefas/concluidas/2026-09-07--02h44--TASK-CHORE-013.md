# TASK-CHORE-013 · Migrar projeto Cloudflare Pages de eu-roteirizo-prototipo para teste-prototipo

## Decisoes tomadas
- Criado o novo projeto `teste-prototipo` no Cloudflare Pages com ramo de producao `main`.
- Atualizadas as referencias do projeto antigo para o novo em `package.json`, `index.html`, `docs-mentor/contexto.json`, `README.md` e documentacao de apoio.
- Executados os quatro gates declarados (`tipos`, `lint`, `testes`, `build`), todos aprovados com saida 0.
- Realizado o deploy e confirmada a resposta HTTP 200 em `https://teste-prototipo.pages.dev`.
- Excluido o projeto antigo `eu-roteirizo-prototipo` no Cloudflare Pages via Wrangler apos a confirmacao do novo no ar.

## O que nao foi feito, e por que
- Nenhum commit, fechamento de tarefa ou push foi executado antes da autorizacao humana por ato (Portoes 2 e 3 do nucleo).
- Nao houve alteracao em regras de negocio ou componentes em `src/`.

## Testes de descoberta
- O primeiro deploy a partir do ramo de trabalho gerou URL de previa (`https://bg-011-carregamento-malha.teste-prototipo.pages.dev`), enquanto a raiz `https://teste-prototipo.pages.dev` retornou 404 por ainda nao ter deploy no ramo de producao (`main`). Um deploy especificando `--branch=main` ativou a raiz com HTTP 200 imediatamente.

## Aprendizados
- Ao criar um projeto novo no Cloudflare Pages, o primeiro deploy deve apontar explicitamente para o branch de producao configurado para vincular o dominio principal `<projeto>.pages.dev`.
