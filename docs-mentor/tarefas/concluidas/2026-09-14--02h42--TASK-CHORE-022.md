# TASK-CHORE-022 · Atualizar mentor-agent para v0.10.0

## Decisoes tomadas

- **Ordem, a mesma das CHORE-019 a 021:** ramo a partir do `main` (`commit_base` 192b034), `task nova`, `puxar` e `iniciar`, e só então `npm i -D github:thiagoroddev/mentor-agent#v0.10.0`, `npx mentor instalar --forcar`, `resolver-gerados` e `verificar`.
- **Leis alteradas, lidas no aviso do `instalar`:**
  - `nucleo.md` (+2 −1): sugestão do humano é hipótese no Portão 1, e o §9 carrega o processo de laboratório;
  - `processos/laboratorio.md` (novo): onde o experimento vive, as travas do spike, a chave desligada e o contrato na fronteira;
  - `processos/tarefa.md` (+15) e `processos/rascunho.md` (+4): o passo Entender, com o problema antes da solução sugerida.
- **O laboratório do piloto ficou declarado** em `contexto.laboratorio`:
  - `caminhos`: `__utilidades-back-office__/**`;
  - `saidas`: `.mentor-saidas/**`, que o `.gitignore` já ignora;
  - a chave `FUNDAMENTAL_ANCORAS_DO_EXPERIMENTO` e o JSON importável do experimento de pontos fundamentais, os dois presos ao teste de contrato da TASK-BG-022.
- **A chave ficou com `remover_em` nulo.** É chave permanente do laboratório, não flag de lançamento, e por isso o `doctor` não cobra prazo dela.
- **Saiu de `melhorias-do-pacote.md` a nota de 13/09/26 21:48**, que a 0.10.0 aplicou.

## O que nao foi feito, e por que

- **O exportador das variantes de inspeção da RF-030 (`corpus.ts`) não entrou no inventário.** Ele marca a âncora que o gerador moveu como escolha do usuário, sem chave, contra a INV-001, e o `corpus.test.ts` prende isso. Registrar esse teste como contrato daria ao defeito a aparência de regra. Virou achado.
- **O roteiro fictício de classificação** (`roteiros-ficticios/roteiro-classificacao.json`, TASK-BG-021) também é importável e não tem teste de contrato da exportação. Sai com a âncora padrão, então não é vazamento. Fica fora do inventário até ter o teste.
- **A vulnerabilidade baixa do esbuild não foi corrigida aqui:** está fora do escopo da atualização. Virou achado.

## Testes de descoberta

Nenhum.

## Aprendizados

- **A varredura para declarar o inventário achou um segundo exportador com o mesmo vazamento da BG-022.** Declarar o laboratório obriga a listar tudo o que dele chega ao produto, e é essa lista que expõe o caso.
- **A trava do spike só vale daqui para frente:** a TASK-SPIKE-003, pausada, vai fechar sob ela.
