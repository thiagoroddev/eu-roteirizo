# TASK-CHORE-014 · Apontar proxy de tiles do mapa para 1-teste-prototipo

## Decisoes tomadas
- O usuario publicou o novo worker `1-teste-prototipo` no Cloudflare Workers e excluiu o worker antigo `tile-proxy`.
- Verificado via terminal que o novo endpoint `https://1-teste-prototipo.thiagorod-dev.workers.dev/tiles/14/4934/9698.png` responde com HTTP 200 (PNG 6.9 KB) e o endpoint antigo responde 404.
- Atualizada a URL do `tileLayer` em `src/components/RouteMap.tsx` e atualizada a assercao do teste em `src/__tests__/components/RouteMap.test.tsx`.
- Formatação Prettier aplicada ao teste de configuracao `deploymentConfig.test.ts`.
- Quatro gates declarados (`tipos`, `lint`, `testes`, `build`) executados e aprovados com saida 0.
- Realizado novo deploy do frontend no Cloudflare Pages `teste-prototipo` (`https://teste-prototipo.pages.dev`).

## O que nao foi feito, e por que
- Nenhum commit ou push foi realizado antes de obter a autorizacao do humano (Portoes 2 e 3 do nucleo).
- Nao houve alteracao no algoritmo de roteamento ou malha viaria.

## Testes de descoberta
- O teste unitario de RouteMap falhou honestamente antes de implementar a troca da URL em RouteMap.tsx, registrando o gate vermelho exigido pelo metodo TDD.

## Aprendizados
- A URL do worker de tiles e' testada diretamente pelo teste unitario de RouteMap, evitando divergencias silenciosas entre o mapa e a infraestrutura.

