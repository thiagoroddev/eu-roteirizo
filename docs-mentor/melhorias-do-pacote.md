# Melhorias do pacote, vistas daqui

> Anotado por `mentor anotar --sobre pacote`. **Nao vira tarefa deste projeto.**
> Entra no relatorio de campo, e o trabalho acontece no repositorio do `mentor-agent`.
>
> As secoes datadas abaixo nasceram em `.mentor/melhorias-do-pacote.md`, escritas a mao antes
> de o comando existir. Movidas para ca em 02/09/26: arquivo acrescentado dentro de `.mentor/`
> conta como divergencia do pacote no `verificar`.

## Regra: corrigir aqui, levar ao pacote depois

Desde 15/09/26 (TASK-CHORE-025), problema encontrado no mentor-agent pode ser corrigido direto neste projeto, sem esperar uma versão nova do pacote. Depois de um tempo, as correções que continuaram valendo são levadas ao repositório do `mentor-agent` e viram versão oficial.

1. **Corrigir aqui.** A correção é feita no `.mentor/` deste projeto, dentro de uma tarefa. Mudar arquivo do `.mentor/` conta como código: o commit precisa do ID da tarefa no título.
2. **Registrar aqui.** Cada correção ganha uma entrada em "Corrigidas aqui", com estado, problema, o que mudou, arquivos, teste e o que fazer no pacote. O problema vem com o caso real: o que aconteceu, onde e quando.
3. **O texto exato fica no git.** A mudança linha por linha é o commit da tarefa, e também sai de `git log -p -- <arquivo>`. Nada é resumido nem copiado para cá.
4. **Cada correção tem teste** em `docs-mentor/melhorias-do-pacote.test.ts`. Se uma atualização do pacote desfizer a correção, o teste falha.
5. **Antes de atualizar o mentor-agent neste projeto, levar ao pacote as entradas com estado "aplicada aqui".** O `instalar --forcar` sobrescreve os arquivos corrigidos. Se a atualização acontecer antes, o git ainda guarda a correção e ela pode ser reaplicada.
6. **O `verificar` mostra uma linha de divergência do pacote enquanto houver correção local**, com os arquivos alterados. É esperado. Arquivo que aparecer nessa linha sem entrada em "Corrigidas aqui" é mudança esquecida e precisa de registro.
7. **Quando uma versão do pacote trouxer a correção,** a tarefa que atualizar o mentor-agent remove a entrada daqui e o teste dela.

Problema visto e ainda não corrigido continua indo para "Anotadas", pelo `mentor anotar --sobre pacote`, que escreve no fim deste arquivo.

## Corrigidas aqui

### 1 · Exemplo da marca `(plano)` no `entrega.md`

- **Estado:** aplicada aqui em 15/09/26, na TASK-CHORE-025, sobre o pacote 0.12.0. Falta levar ao pacote.
- **Problema:** `.mentor/processos/entrega.md`, seção "Planejamento independente e sessões isoladas", item 1, ensinava o título do PR de planejamento com a marca no fim: `docs: novo fluxo de checkout (plano)`. O `pronto-para-merge` usa `MARCA_PLANO_NO_TITULO`, de `.mentor/scripts/tipos.ts` (`/^[a-z]+\(plano\)!?:\s*\S/i`), que só aceita a marca na posição de escopo: `docs(plano): ...`. Seguindo o exemplo do documento, a esteira recusou o PR #48 deste projeto com "o titulo nao cita tarefa (TASK-X-NNN), nem a marca (light), nem (plano)" (15/09/26, ramo `plan/2026-09-15-base-final-motor-e-app-mobile`). As duas formas nasceram no mesmo commit da 0.12.0 (`e9d3fcb`, 14/09/26). O plano da versão dizia só "marca explícita `(plano)`", sem a posição, e nenhum cenário do pacote roda um título com `(plano)`.
- **O que mudou:** a linha passou a dizer que o PR de planejamento "leva a marca `(plano)` na posição de escopo do título, como `(light)`", com o exemplo `docs(plano): novo fluxo de checkout`. A regex não mudou: `(light)` usa a mesma forma, e o núcleo define o commit como `<tipo>(<marca>): <descrição>`.
- **Arquivos:** `.mentor/processos/entrega.md`.
- **Teste:** "o exemplo de PR de planejamento do entrega.md passa na marca (plano)". O teste lê o exemplo do próprio documento e o passa pela regex que o `pronto-para-merge` usa.
- **No pacote:** trocar a mesma linha em `.mentor/processos/entrega.md`. Criar um cenário que rode `pronto-para-merge --titulo` com o título do exemplo documentado, num ramo que só muda planejamento, e espere "Pronto para merge: PR de planejamento (plano) validado."

### 2 · Checagem de maiúsculas do `verificar` subia até a raiz do disco

- **Estado:** aplicada aqui em 15/09/26, na TASK-CHORE-025, sobre o pacote 0.12.0. Falta levar ao pacote.
- **Problema:** `existeComGrafiaExata`, em `.mentor/scripts/cmd-verificar.ts`, confere se cada pasta do caminho de um link existe com a grafia exata, e subia até a raiz do disco. Quando o projeto é aberto por um caminho digitado com outras maiúsculas (por exemplo `cd e:\Repositorios\...`, com a pasta real `E:\repositorios\...`), as pastas acima do projeto não batem letra por letra, e o `verificar` acusa todos os links com "so resolve porque este sistema de arquivos ignora maiusculas". Medido em 14/09/26 num clone deste projeto na pasta temporária da sessão, em que o disco tinha `e--repositorios-...` e o caminho usado dizia `E--repositorios-...`: 79 falsos positivos, inclusive nos links do próprio `.mentor/guia`. Reproduzido em 15/09/26 com um projeto criado por `mentor init` na pasta temporária do Windows e aberto pelo mesmo caminho em maiúsculas. A função é igual desde a 0.2.0 (`1667c8e`, 31/08/26). A anotação anterior deste arquivo dizia, por engano, que o problema vinha da 0.12.0.
- **O que mudou:** a função recebe a raiz do projeto e para de subir ao chegar nela ou numa pasta acima dela. As pastas abaixo da raiz continuam conferidas letra por letra. A raiz e o que fica acima dela ficam como vieram do terminal, porque não fazem parte do repositório. As duas chamadas, a dos links em markdown e a das referências de `referencias.json`, passam `c.raiz`.
- **Arquivos:** `.mentor/scripts/cmd-verificar.ts` (a função `existeComGrafiaExata`, as duas chamadas e `sep` importado de `node:path`).
- **Testes:** "caixa diferente acima da raiz do projeto nao reprova link certo" e "caixa errada dentro do projeto continua acusada". Os dois criam um projeto com `mentor init` na pasta temporária, com um link certo e um link com maiúscula errada, e rodam o `verificar` pelo caminho com as letras acima do projeto trocadas. Só rodam em sistema que ignora maiúsculas, como o Windows; no Linux são pulados.
- **No pacote:** aplicar a mudança de `cmd-verificar.ts` e criar um cenário com o mesmo projeto e as mesmas duas verificações, rodando só em sistema que ignora maiúsculas.

### 3 · Data e lembretes regravados no `contexto.json` a cada comando

- **Estado:** aplicada aqui em 15/09/26, na TASK-CHORE-026, sobre o pacote 0.12.0. Falta levar ao pacote. Resolve em parte a anotação de 15/09/26 04:26, abaixo: as contagens continuam no arquivo.
- **Problema:** dois campos do `docs-mentor/contexto.json` versionado mudavam sem mudança real no projeto.
  - `_meta.atualizado_em`: `atualizarContagens`, em `.mentor/scripts/vistas.ts`, gravava a data e a hora atuais toda vez que rodava, mesmo sem nada ter mudado. Ela roda dentro de `regenerarTudo`, chamada por `task`, `req`, `ref`, `fila`, `riscos`, `auditar`, `init`, `gerar` e `resolver-gerados`.
  - `lembretes`: o `doctor`, em `.mentor/scripts/cmd-doctor.ts`, gravava no arquivo a lista de avisos a cada execução, e o `resolver-gerados` a zerava em todo merge. A lista ficava indo e voltando entre vazia e preenchida.
  - Com isso, qualquer ramo que rodasse um comando do mentor mudava o arquivo, e dois PRs abertos ao mesmo tempo conflitavam. Medido em 15/09/26: os PRs #49 e #50 deste projeto conflitaram só em `atualizado_em`, e no fechamento do #52 o `doctor` deixou o `contexto.json` alterado só com os lembretes.
  - Nenhum código do pacote lê `atualizado_em` nem `lembretes`. A data da última mudança sai do `git log`, e os lembretes aparecem na tela do `doctor`, que o núcleo manda rodar no início de toda sessão.
- **O que mudou:**
  - `atualizarContagens` grava `_meta.atualizado_em` como `null`, e não mais a data atual.
  - O `doctor` grava `lembretes` como lista vazia e continua mostrando os avisos na tela.
  - Os dois valores fixos também limpam a data congelada e os lembretes antigos de quem vem de versão anterior.
  - As contagens (`contagens.*`) continuam no arquivo: só mudam com mudança real, e o `resolver-gerados` resolve esses conflitos.
- **Arquivos:** `.mentor/scripts/vistas.ts` (fim de `atualizarContagens` e o import de `agoraIso`, que ficou sem uso) e `.mentor/scripts/cmd-doctor.ts` (fim da função `doctor`).
- **Testes:** "gerar duas vezes sem mudanca real nao muda o contexto.json", "doctor duas vezes nao muda o contexto.json" e "doctor mostra lembretes sem grava-los no contexto.json". Os três criam um projeto com `mentor init` na pasta temporária e comparam o `contexto.json` byte a byte entre as execuções.
- **No pacote:** aplicar as duas mudanças e criar um cenário que rode `gerar` duas vezes e `doctor` duas vezes e compare o `contexto.json`. Avaliar também a sugestão da anotação de 15/09/26 04:26, que tira as contagens do arquivo versionado. Com as contagens fora, `atualizado_em` e `lembretes` podem sair do esquema de vez.

### 4 · `pronto-para-merge` confundia tarefa criada com tarefa entregue

- **Estado:** aplicada aqui em 15/09/26, na TASK-CHORE-027, sobre o pacote 0.12.0. Falta levar ao pacote.
- **Problema:** `prontoParaMerge`, em `.mentor/scripts/cmd-merge.ts`, extraia qualquer `TASK-X-NNN` do titulo antes de olhar os escopos `(plano)` e `(light)`. No PR #55 deste projeto, ramo `plan/2026-09-15-triagem-aud-004`, o titulo `docs(light): registrar e triar a AUD-004, criar a TASK-DOC-011 e a DT-012 e anotar melhorias do pacote` citava a tarefa documental que o proprio PR criava aberta. A execucao #129 do Actions recusou o merge com `TASK-DOC-011 esta "aberta": feche com "task finalizar" antes do merge`, embora fechar a tarefa no PR que a cria contradiga o fluxo de planejamento. Reproduzido localmente em 15/09/26: retirar apenas o ID do assunto deixava o check verde. Trocar para o escopo correto `(plano)` ainda reprovava, porque o allowlist nao incluia `docs-mentor/auditorias/` nem `docs-mentor/dividas/`, os dois registros que `auditar resolver` altera durante a triagem.
- **O que mudou:** os escopos estruturados `(plano)` e `(light)` sao classificados antes de IDs mencionados no assunto. Sem uma dessas marcas, os IDs continuam exigindo tarefas concluidas. O allowlist de `(plano)` ganhou somente `auditorias/` e `dividas/`, nas pastas atual e legada; codigo, gates e transicoes de tarefa continuam barrados pelas regras existentes.
- **Arquivos:** `.mentor/scripts/cmd-merge.ts`.
- **Testes:** "escopos explicitos prevalecem sobre IDs citados no assunto", "PR de planejamento aceita os registros produzidos pela triagem de auditoria" e "PR de entrega continua exigindo a tarefa concluida". Os testes criam um repositorio temporario, fazem um commit de triagem com auditoria, divida, `TASK-DOC-011` aberta e a vista da reserva, usam os titulos `(plano)` recomendado e `(light)` visto no PR #55, ambos citando essa tarefa, e depois conferem separadamente que `fix(TASK-CHORE-999)` ainda reprova com a tarefa aberta.
### 5 · Reestruturação Mentor V5 (Etapas 00 a 06)

- **Estado:** aplicada aqui em 16/09/26, sobre o pacote 0.12.0.
- **Problema:** O Mentor sofria de cerimônia excessiva, repetição de suítes completas e gates idênticos no hook de pre-push sem reaproveitamento de evidência, bloqueios por divergências locais no `verificar`, duplicação de planos narrativos entre ferramentas/sessões, e ausência de base verificável de tipos e lint para os scripts do próprio `.mentor/`.
- **O que mudou:**
  - **Etapa 00 (Base Verificável):** scripts `typecheck:mentor`, `lint:mentor`, `test:mentor`, `tsconfig.mentor.json` e `eslint.mentor.config.js`.
  - **Etapa 01 (Planejamento Portátil):** `cmd-plano.ts` (`mentor plano registrar`, `importar`, `planos`, `mentor task vincular-plano <ID>`), permitindo que planos definidos em artefatos externos ou ferramentas distintas sejam reutilizados por referência e verificados por SHA-256 sem duplicação de narrativa.
  - **Etapa 02 (Executor de Gates Unificado):** `executor-gates.ts` (`executarGateDaTarefa`, `executarGatesDaTarefa`, `executarGatesDoProjeto`) unificando a execução, parando no primeiro erro, salvando logs detalhados em `docs-mentor/.evidencias/logs/` e invalidando execuções com 0 testes.
  - **Etapa 03 (Identidade dos Insumos e Cache Conservador):** `fingerprint.ts` calcula o hash da árvore de insumos isolando o index via `GIT_INDEX_FILE` temporário, incluindo testes sob `docs-mentor/` e excluindo apenas evidências efêmeras. Cache conservador grava `log_ref`, `chave_cache`, e só reaproveita gates válidos e idênticos.
  - **Etapa 04 (Pre-push e Integração com CI):** `cmd-hooks.ts` executa verificações baratas primeiro (bypass de WIP, exclusão remota, push direto na main, branch suja em código), e `cmd-merge.ts` valida refs base/head via `git rev-parse` com matriz estrita para PRs `(light)` e `(plano)`.
  - **Etapa 05 (Consultas Somente Leitura e Registro de Patches):** `cmd-doctor.ts` tornado 100% somente-leitura; `cmd-patches.ts` e `docs-mentor/patches-do-pacote.json` rastreiam patches locais determinísticos, fazendo `node mentor.mjs verificar` sair verde (APROVADO) com zero ruído.
  - **Etapa 06 (Processo Compacto e Governança por Risco):** Suporte ao perfil `Standard compacto` (`--cerimonia Standard --perfil compacto`), dispensando ensaios de alternativas sem decisão arquitetural real; categorização sensível em `sensivel.ts` cobrindo segurança, persistência e execução de gates contra dispensas indevidas; convivência harmônica com tarefas legadas.
- **Arquivos:** `.mentor/scripts/` (`executor-gates.ts`, `fingerprint.ts`, `cmd-plano.ts`, `cmd-patches.ts`, `sensivel.ts`, `tipos.ts`, `cmd-tarefa.ts`, `cmd-doctor.ts`, `cmd-hooks.ts`, `cmd-merge.ts`, `cmd-verificar.ts`, `cli.ts`), `.mentor/nucleo.md`, `.mentor/processos/tarefa.md`, `.mentor/esquemas/tarefa.json`, `docs-mentor/patches-do-pacote.json`, `package.json`, `tsconfig.mentor.json`, `eslint.mentor.config.js`.
- **Testes:** Suíte completa em `docs-mentor/melhorias-do-pacote.test.ts` (Etapas 00 a 06).
- **No pacote:** Integrar a arquitetura V5 ao pacote upstream `mentor-agent`.

### 6 · Seção de desfecho e validação real obrigatória nas narrativas de estudo humano

- **Estado:** aplicada aqui em 17/09/26, na TASK-CHORE-028, sobre o pacote 0.12.0. Falta levar ao pacote.
- **Problema:** Tarefas eram finalizadas gerando o arquivo `--estudo-humano.md` sem documentar o desfecho da execução nem os aprendizados e peculiaridades de validação observados em campo (como concorrência IndexedDB, armadilhas de cache, comportamento real nos testes manuais e resultado dos gates). Isso comprometia o objetivo do arquivo de estudo humano como memória viva de engenharia para aprendizado humano e investigações de regressão.
- **O que mudou:**
  - **Processo & Núcleo:** `.mentor/nucleo.md` (§ 2 e § 4), `.mentor/processos/tarefa.md` (Travas no Fechamento item 10 e Fechamento) e `AGENTS.md` instruem a IA a redigir ativamente a seção `## Desfecho e Validação Real` antes do Portão 2.
  - **Templates de Início:** `task iniciar` em `.mentor/scripts/cmd-tarefa.ts` gera o esqueleto de `## Desfecho e Validação Real` nos modelos compacto, standard e spike.
  - **Trava de Segurança:** `task finalizar` em `.mentor/scripts/cmd-tarefa.ts` verifica a presença e o preenchimento não vazio da seção `## Desfecho` (ou `## Desfecho e Validação Real`), bloqueando a finalização com mensagem orientadora caso esteja ausente ou vazia.
- **Arquivos:** `.mentor/nucleo.md`, `.mentor/processos/tarefa.md`, `.mentor/scripts/cmd-tarefa.ts`, `AGENTS.md`.
- **Testes:** Suíte F07 (`F07-1`, `F07-2`, `F07-3`) em `docs-mentor/melhorias-do-pacote.test.ts` garantindo recusa sem a seção, recusa com seção vazia, e aprovação quando preenchida com geração de `--estudo-humano.md`.
- **No pacote:** Incorporar as regras de processo em `nucleo.md` e `processos/tarefa.md`, os templates em `cmd-tarefa.ts:iniciar` e a trava em `cmd-tarefa.ts:finalizar`.

## Anotadas

As versões 0.4.0 a 0.12.0 do mentor aplicaram as melhorias sugeridas por este arquivo. A 0.12.0 (TASK-CHORE-024) aplicou as sete notas de 12/09 a 14/09/26 (Frentes A a G). As duas notas de 15/09/26 viraram as correções 1 e 2 acima. Anotações novas entram abaixo desta linha.

- **15/09/26 04:26** · O docs-mentor/contexto.json versionado guarda campos que o proprio mentor reescreve em quase todo comando: _meta.atualizado_em (data e hora) e contagens como tarefas_concluidas. Dois ramos que rodam comandos do mentor mudam essas linhas, e o merge do segundo sempre conflita, mesmo sem mudanca real em comum. Medido em 15/09/26 no Eu Roteirizo: o PR #49 (TASK-CHORE-025, que mudou atualizado_em e tarefas_concluidas 37 para 38) e o PR #50 (uma frase em versionamento.estrategia_de_ramos, que tambem mudou atualizado_em) conflitaram so em atualizado_em. A resolucao exigiu git merge origin/main, mentor resolver-gerados, um commit de merge, outro push e outra rodada de 3 min da esteira. Sugestao: tirar do arquivo versionado o que se deriva. A data de atualizacao sai do git log; as contagens se calculam na leitura (doctor, gerar) ou vao para uma vista gerada fora do contexto.json, com merge=ours no .gitattributes, como ja acontece com contexto.md e backlog.md.

- **15/09/26 05:30** · O aviso do auditar preparar diz 'o dossie chegou ao teto de 120000 caracteres', mas o teto (LIMITE_DIFF, em .mentor/scripts/cmd-auditar.ts) so conta o diff das tarefas: o laco de empacotamento da funcao preparar soma patch.total e patch.texto e deixa de fora as regras, os planos e os fatos que tambem entram no dossie. O comentario da constante chama o teto de 'a janela de contexto do auditor', entao a conta nao mede o que diz medir, e o aviso afirma uma coisa falsa. Medido em 15/09/26 no Eu Roteirizo: a AUD-003 saiu com 8 tarefas, 6 ficaram para a proxima 'pelo teto de 120000', e o AUD-003-dossie.md gerado tem 157.463 caracteres, cerca de 37 mil fora da conta. Impacto baixo: 157 mil caracteres cabem na janela de uma sessao de IA; o efeito e o lote sair de tamanho diferente do anunciado. Sugestao: ou o teto passa a medir o dossie montado (texto fixo, planos, fatos e diffs), ou o aviso e o comentario passam a dizer que o limite e do diff do lote. Com cenario que rode auditar preparar e compare o tamanho do dossie gerado com o teto que o aviso anuncia. Confirmado na AUD-004, preparada em 15/09/26 05:38 com as 6 tarefas restantes: nenhuma ficou para depois e nao houve aviso de teto, mas o AUD-004-dossie.md tem 161.570 caracteres, 41.570 acima dos 120000.

- **15/09/26 06:42** · O dossie da auditoria esconde a evidencia gravada por task criterio, e o fato mecanico chama de NAO VERIFICADO todo criterio cujo teste comeca com 'nao se aplica', mesmo quando ha evidencia. Onde: .mentor/scripts/cmd-auditar.ts. Na montagem do dossie, a linha que escreve cada criterio de aceite usa so cr.texto e cr.teste; o campo cr.evidencia (comando, codigo_saida, saida, executado_em, gravado por task criterio em cmd-tarefa.ts) nao e lido em nenhum ponto do arquivo. Em fatosMecanicos, semTeste filtra so por cr.teste.startsWith('nao se aplica') e escreve 'Criterio sem verificacao reproduzivel e' criterio NAO VERIFICADO'. Caso medido em 15/09/26 no Eu Roteirizo, AUD-003: o lote tinha 12 criterios 'nao se aplica' em 7 tarefas. 10 tinham evidencia gravada: 9 com comando executado e codigo de saida (TASK-CHORE-019 criterio 0; CHORE-020 criterios 0, 1 e 2; CHORE-021 criterios 0, 1 e 2, sendo o 2 com codigo 1, que era a recusa esperada do pronto-para-merge; CHORE-022 criterios 0 e 1) e 1 so com texto por --saida, comando null (TASK-BG-022 criterio 6). So 2 nao tinham nada (TASK-CHORE-017 criterio 0 e TASK-BG-021 criterio 3). O dossie mostrou os 12 do mesmo jeito, os fatos chamaram os 12 de NAO VERIFICADO, e o auditor listou em nao_verificado criterios que tinham saida gravada. O humano teve de descartar achado sobre evidencia que existia. Sugestao: (1) o dossie escreve, abaixo de cada criterio, a evidencia gravada: comando, codigo de saida, horario e as primeiras linhas da saida; sem evidencia, escreve 'sem evidencia'; (2) o fato conta como NAO VERIFICADO so o criterio 'nao se aplica' sem evidencia, e conta a parte o criterio com evidencia so em texto (comando null), que e declaracao e nao execucao; (3) codigo de saida diferente de 0 aparece no fato, para o auditor conferir se a falha era a esperada. Cenario: tarefa com tres criterios 'nao se aplica': um com task criterio --comando que sai 0, um com task criterio --saida e um sem evidencia. O dossie precisa mostrar a evidencia dos dois primeiros, e os fatos precisam dizer 1 criterio sem evidencia e 1 com evidencia so em texto. Confirmado na AUD-004, preparada em 15/09/26 05:38: as TASK-BG-023, TASK-CHORE-024, TASK-CHORE-025 e TASK-CHORE-026 tinham 9 criterios 'nao se aplica' fora da validacao manual. 6 tinham evidencia gravada: 5 com comando e codigo de saida 0 (BG-023 criterio 2, node mentor.mjs verificar, 14/09/26 07:22; CHORE-024 criterios 0 a 3, com verificar, doctor e dois node -e, 15/09/26 00:43) e 1 so com texto, comando null (BG-023 criterio 3, a bateria npm run test:auto-anchors). Os outros 3 nao tinham evidencia e remetiam a leitura do humano na validacao manual (CHORE-025 criterios 3 e 4, CHORE-026 criterio 3). O dossie mostrou os 9 do mesmo jeito, os fatos chamaram todos de NAO VERIFICADO, e o auditor os pos em nao_verificado e abriu a AUD-004-R03.

- **15/09/26 06:42** · A auditoria em lote cobra evidencia de teste de tarefa que ja fechou e ja esta no main, quando nao ha mais o que fazer com o achado. Caso medido em 15/09/26 no Eu Roteirizo: a AUD-003 foi preparada as 05:25 sobre 8 tarefas concluidas entre 13/09/26 00:20 (TASK-CHORE-017) e 14/09/26 02:42 (TASK-CHORE-022), todas ja no main pelos PRs #33 e #35 a #41. O humano descartou todos os achados sobre evidencia de teste e de validacao manual (AUD-003-R03 e os criterios sem evidencia da lista nao_verificado) com a decisao 'ja foi, tarefa passada; o erro e fazer auditoria em lote'. Refazer evidencia de tarefa entregue ha dias exige reabrir trabalho encerrado, e ninguem reabre: o achado so ocupa o humano. O furo que deixa isso chegar a auditoria esta no task finalizar (.mentor/scripts/cmd-tarefa.ts): a checagem 'Todo criterio de aceite nomeia um teste' so recusa teste vazio; 'nao se aplica: <motivo>' passa sem nenhuma evidencia de task criterio. Foi assim que a TASK-BG-021 (criterio 3) fechou em 13/09/26 03:30, e o codigo da 0.12.0 ainda aceita. Sugestao: (1) o task finalizar recusa criterio com teste 'nao se aplica' sem evidencia gravada por task criterio, com a mensagem 'criterio[i] sem teste e sem evidencia: rode task criterio <ID> <i> --comando "..."'; (2) com essa trava, a evidencia de teste passa a ser cobrada enquanto a tarefa esta aberta, antes do portao 2, e o texto do dossie deixa de mandar o auditor abrir pendencia sobre evidencia de teste de tarefa do lote: o que faltar vai so para nao_verificado. A auditoria em lote fica com o que precisa de olho independente sobre o diff: criterio contradito pelo codigo, seguranca, dado pessoal, performance e requisito contradito. Cenario: task finalizar de tarefa com um criterio 'nao se aplica' sem evidencia precisa recusar; depois de task criterio --comando no mesmo criterio, precisa aceitar. Confirmado na AUD-004 (15/09/26), agora com validacao manual. A TASK-RF-045 fechou em 14/09/26 12:48, ja no main pelo PR #46, com validacao_manual APROVADO gravada por task validar --evidencia: um resumo escrito pela IA da sessao do humano no Samsung M35 (linha do tempo com P2, P3 e P4, distancias 730, 805 e 585 m, P3 aberta) e a resposta dele, 'Perfeito'. O criterio pedia tema claro e escuro, totais, sugestao e ignorados; o resumo nao citava esses itens, e o auditor bloqueou (AUD-004-B02) pela regra 'gate sem evidencia'. O humano descartou com esta definicao: 'tudo que depende de eu ver e usar o app eu valido dizendo esta tudo ok, isso e validacao de usuario, usabilidade'. A resposta de aprovacao do humano cobre o roteiro inteiro que foi apresentado a ele; o que deixou de citar os itens foi o resumo que a IA grava, e nao a validacao. Hoje o validar (funcao validar, em .mentor/scripts/cmd-tarefa.ts) guarda so esse resumo: exige --evidencia com pelo menos 30 caracteres e nao guarda o roteiro apresentado nem a resposta do humano. Sugestao, que completa a (2) acima: task validar --aprovado passa a gravar dois campos, o roteiro apresentado ao humano (os itens do criterio de validacao manual) e a resposta dele, palavra por palavra; o dossie mostra os dois, e o texto do dossie diz ao auditor que a resposta de aprovacao do humano vale para todos os itens do roteiro e que resumo incompleto nao gera pendencia. Cenario: tarefa com criterio de validacao manual de tres itens, validada com a resposta 'esta tudo ok'; o dossie precisa mostrar os tres itens e a resposta, e nenhum fato do dossie pode chamar algum dos tres de nao verificado.

- **15/09/26 06:42** · Requisito escrito a mao no requisitos.json, ja como implementado, passa sem nenhuma trava, e criterio de requisito nunca precisa de teste. Caso medido em 15/09/26 no Eu Roteirizo (AUD-003-R02): o commit da TASK-BG-022 (192b034, PR #40) criou a RN-23, prioridade essencial, com criado_em 13/09/26 23:26, seis minutos depois de a tarefa fechar (concluida_em 23:20). A RN-23 entrou ja com status implementado, implementado_em 23:26, tarefas [TASK-BG-016, TASK-BG-022], requisitos_relacionados [RF-34, RF-52] e cinco criterios de aceite, e o mesmo commit acrescentou RN-23 as regras_aplicaveis da RF-34. Nenhum comando grava esses valores: req nova (.mentor/scripts/cmd-requisito.ts) cria sempre com status pendente, tarefas [] e implementado_em null, e nao tem opcao para requisitos_relacionados; o task finalizar (.mentor/scripts/cmd-tarefa.ts) so marca implementado o requisito listado em tarefa.requisitos, e a BG-022 fechou com requisitos []. O comentario do proprio finalizar diz 'O vinculo requisito <-> tarefa e gravado aqui, nunca pela IA'. A IA gravou a mao e so avisou o humano de que tinha criado a regra; o humano nao tinha como avaliar. Dos cinco criterios, so o primeiro tem teste (src/__tests__/utils/routing/vehicleStop.test.ts); os outros quatro nao aparecem em criterio de aceite de nenhuma tarefa. Tres furos deixaram passar: (1) referencias(), em .mentor/scripts/cmd-verificar.ts, so acusa requisito implementado sem tarefa vinculada, e nao confere se o status veio do finalizar; (2) a trava de escopo do task finalizar (lista de ignorados com 'docs-mentor/') e o fato 'arquivos fora do plano.muda' de fatosMecanicos, em cmd-auditar.ts (filtro !a.caminho.startsWith(docs)), pulam todo arquivo de docs-mentor/, com o argumento de que divida e risco nascem de comando; requisitos.json e invariantes.json nao nascem so de comando e mudam sem aviso; alem disso, a RN-23 foi gravada depois do finalizar, quando a trava de escopo ja tinha rodado; (3) o esquema do requisito guarda criterios_aceite como lista de texto, sem o teste de cada um. Pedido do humano: nao deixar criterio sem teste. Sugestao: (a) cada criterio de requisito passa a ser {texto, teste}, no mesmo formato do criterio de aceite da tarefa; requisito antigo migra com teste null; (b) o task finalizar recusa marcar implementado requisito que tenha criterio com teste null ou vazio, dizendo qual criterio; (c) o verificar acusa requisito implementado com criterio sem teste, e requisito implementado cujo implementado_em nao e igual ao concluida_em de nenhuma tarefa da lista tarefas; como o verificar roda na esteira, isso pega tambem a gravacao feita depois do finalizar. Medido neste projeto, a regra da data acusaria hoje dois requisitos: a RN-23 e a RF-63 (implementado_em 07/07/26, vinculada a TASK-REF-013, que nao existe em tarefas/concluidas); por isso, tarefa da lista que nao existe em concluidas vira um achado separado, 'tarefa vinculada nao encontrada', e nao o achado de data; (d) a excecao de docs-mentor/ na trava de escopo do finalizar e no fato do auditar deixa de valer para requisitos.json e invariantes.json. Cenario: requisito gravado a mao com status implementado, implementado_em sem tarefa correspondente e um criterio sem teste; o verificar precisa acusar os dois problemas, e o task finalizar de tarefa vinculada a ele precisa recusar.

- **15/09/26 06:42** · Requisito sem nenhum criterio de aceite vira implementado sem aviso. Caso medido em 15/09/26 no Eu Roteirizo (AUD-003-O10): a RF-58 foi criada por req nova em 12/09/26 21:20 (commit 8236ece, 'registrar RF-57/58/59') com criterios_aceite [], porque em .mentor/scripts/cmd-requisito.ts a opcao --criterios e opcional e, sem ela, o comando grava lista vazia e nao avisa. A TASK-RF-044 foi aberta com requisitos [RF-58] e fechou em 13/09/26 21:53; o task finalizar (.mentor/scripts/cmd-tarefa.ts) marcou a RF-58 implementado sem olhar os criterios. O humano pediu que a IA criasse o requisito e o implementasse; a IA obedeceu sem propor criterio e sem avisar que o requisito ficava sem nenhum, e nenhum comando cobrou. Hoje 3 dos 17 requisitos implementados do projeto estao assim: RF-57, RF-58 e RF-59. O verificar tambem nao acusa. Sugestao: (a) req nova recusa criar requisito sem --criterios, com a mensagem 'requisito sem criterio de aceite nao tem como ser verificado: use --criterios "a|b"'; (b) task nova --requisitos recusa vincular requisito com criterios_aceite vazio; (c) o task finalizar recusa marcar implementado requisito com criterios_aceite vazio; (d) o verificar acusa requisito implementado com criterios_aceite vazio, para os que ja existem. Cenario: requisito gravado com criterios_aceite [] e tarefa aberta vinculada a ele; task finalizar precisa recusar dizendo o ID do requisito, e o verificar precisa acusar o requisito.

- **15/09/26 06:42** · O detector de tarefa sensivel decide por palavra solta em texto livre, e erra para os dois lados. Dele saem, no dossie, a frase 'Regra 4 atendida' ou a ausencia de qualquer aviso, e a mesma funcao decide a validacao manual obrigatoria no task validar e no task finalizar. Onde: categoriasSensiveis, em .mentor/scripts/sensivel.ts, aplica regex sobre titulo + plano.muda + plano.impacto (CALCULO = /c[aá]lcul|algor[ií]tm|f[oó]rmula|heur[ií]stic/, PERSISTENCIA = /schema|migration|migra[cç][aã]o|persist|banco|db_|indexeddb|storage/) e marca ui quando plano.muda cita arquivo .tsx, .jsx, .vue, .svelte, .css ou .html. Casos medidos em 15/09/26 no Eu Roteirizo, AUD-003: (1) falso negativo total na TASK-BG-021, que mudou a classificacao comercial/residencial e declarou no impacto que isso muda a coluna Horario Comercial? e a contagem do Sumario: nenhuma palavra casa, e o dossie nao deu fato de sensibilidade nenhum, embora a AUD-002-B01 tenha bloqueado essa mesma mudanca por Regra 4; (2) falso negativo de calculo na TASK-BG-022, que trocou a regra de distancia da parada padrao do veiculo (defaultVehicleStop: teto de 250 m trocado por folga de 10 m) e com isso reancora paradas padrao de roteiros ja salvos: saiu so ui, e so porque plano.muda cita src/pages/MapPage.tsx, onde a mudanca foi apenas um comentario; o fato disse 'validacao manual atendida' em vez de 'Regra 4 atendida'; (3) falso positivo por negacao na TASK-RF-044, cujo impacto diz 'nenhum modelo, persistencia ou calculo de rota': saiu 'persistencia, calculo, ui' e 'Regra 4 atendida'. Sugestao: (a) as categorias que nao vem do tipo da tarefa (persistencia, calculo e ui) passam a ser declaradas no plano, no portao 1, num campo plano.sensivel com a lista dessas categorias ou com 'nenhuma' e o motivo; regra de negocio, nao funcional e spike continuam vindo do tipo; (b) task iniciar escreve o campo com marcador PREENCHER, e o task finalizar recusa plano com o marcador; (c) categoriasSensiveis passa a devolver o declarado mais as categorias do tipo, e o dossie, o validar e o finalizar usam essa lista; (d) a regex atual continua so como alerta: quando ela acha categoria que o plano nao declarou, o finalizar e o dossie mostram 'o texto do plano cita calculo e plano.sensivel nao declara', sem decidir nada. Cenario: tres tarefas com o titulo, o muda e o impacto da BG-021, da BG-022 e da RF-044; plano.sensivel declarado assim: BG-021 [calculo], BG-022 [calculo, persistencia], RF-044 [ui]. O fato precisa seguir o declarado nas tres; o alerta precisa aparecer na BG-022 (a regex acha ui, nao declarada) e na RF-044 (a regex acha persistencia e calculo, nao declaradas), e nao aparecer na BG-021 (a regex nao acha nada).

- **15/09/26 06:43** · O fato 'arquivos fora do plano.muda' da auditoria acusa arquivo que o plano declarou quando a linha nao comeca pelo caminho. Onde: extrairCaminhosDeclarados, em .mentor/scripts/arquivos.ts, fica com o trecho antes do primeiro ' - ', ' — ' ou ': '; se a linha nao tem nenhum desses separadores, fica so com a primeira palavra. caminhoCorrespondeDeclaracao, no mesmo arquivo, compara o caminho do git com esses trechos por igualdade, por sufixo precedido de '/' ou por glob. Caso medido em 15/09/26 no Eu Roteirizo (AUD-003-O08): a TASK-CHORE-017 (13/09/26, pacote 0.6.0) declarou 'doctor.ts (arquivo .mentor/scripts/cmd-doctor.ts) reinstalado via mentor instalar --forcar (pacote v0.6.0); linha comeca pelo sufixo sem hifen porque o finalizar corta o caminho no hifen', que era o contorno do parser antigo que cortava no hifen (AUD-002-R09). A linha nao tem separador, o parser le 'doctor.ts', e '.mentor/scripts/cmd-doctor.ts' nao termina em '/doctor.ts'. O dossie da AUD-003 afirmou '1 arquivo(s) nos commits da tarefa sem constar no plano.muda: .mentor/scripts/cmd-doctor.ts'. As linhas iguais de cmd-fila.ts e cmd-tarefa.ts nao foram acusadas so porque esses dois arquivos estao iguais ao manifesto ('pacote intacto') e saem da conta; cmd-doctor.ts tem correcao local desde a TASK-CHORE-026. A mesma funcao serve a trava de escopo do task finalizar, que tem o mesmo defeito. Sugestao: caminhoCorrespondeDeclaracao passa a receber tambem as linhas originais de plano.muda, e um arquivo do diff conta como declarado quando o caminho relativo dele aparece literalmente em qualquer posicao de uma linha, alem das regras atuais; a mudanca vale para o fato do auditar e para a trava do finalizar. Cenario: plano.muda com a linha exata da CHORE-017 e commit que muda .mentor/scripts/cmd-doctor.ts; nem o fato do auditar nem a trava do finalizar podem acusar o arquivo.

- **15/09/26 07:54** · O dossie da auditoria esconde o diff de docs-mentor/contexto.json e de docs-mentor/melhorias-do-pacote.md mesmo quando a tarefa declarou a mudanca no plano.muda e um criterio de aceite depende dela. Onde: .mentor/scripts/cmd-auditar.ts, na classificacao dos arquivos de cada tarefa (tipo MotivoDeExclusao): arquivo listado em VISTAS_GERADAS ('contexto.json', 'contexto.md', 'referencias.md', 'requisitos/implementados.md', 'requisitos/pendentes.md') recebe o motivo 'vista gerada', e melhorias-do-pacote.md recebe 'nota'; os dois saem do diff mostrado ao auditor sem olhar o plano da tarefa. So que contexto.json nao e so vista: ele tem blocos que a tarefa escreve a mao, como laboratorio.chaves, laboratorio.artefatos_importaveis, gates e versionamento. Caso medido em 15/09/26 no Eu Roteirizo, AUD-004: (1) a TASK-BG-023 declarou no plano.muda 'docs-mentor/contexto.json - laboratorio.chaves ganha INSPECAO_ANCORAS_DO_EXPERIMENTO; artefatos_importaveis ganha o JSON da inspecao RF-030 com teste de contrato', e o terceiro criterio de aceite dela e 'node mentor.mjs verificar aprova a chave e o artefato importavel registrados no contexto'; o dossie listou o arquivo com 26 linhas como 'vista gerada' e nao mostrou nenhuma; (2) a TASK-CHORE-024 (16 linhas), a TASK-CHORE-025 (38 linhas) e a TASK-CHORE-026 (17 linhas) declararam mudanca em melhorias-do-pacote.md e tem criterio de aceite sobre o texto desse arquivo; o dossie mostrou as tres como 'nota', sem o texto. O auditor pos os quatro casos em nao_verificado e abriu a AUD-004-R03. Sugestao: arquivo que cairia em 'vista gerada' ou em 'nota' entra no dossie com o diff completo quando o plano.muda da tarefa o declara, pela mesma comparacao de caminho que o fato 'arquivos fora do plano.muda' ja usa (caminhoCorrespondeDeclaracao, em .mentor/scripts/arquivos.ts); sem declaracao, continua fora. Cenario: duas tarefas no mesmo lote mudam docs-mentor/contexto.json; a primeira declara o arquivo no plano.muda e muda laboratorio.chaves, a segunda so muda contagens e nao o declara. O dossie precisa mostrar o diff de contexto.json na primeira e continuar listando o arquivo como 'vista gerada' na segunda. O mesmo cenario com melhorias-do-pacote.md precisa mostrar o diff na tarefa que o declara e 'nota' na que nao declara.

- **15/09/26 07:54** · A trava da terceira reconfirmacao de restricao fundadora (regra M3, que entrou na 0.12.0) procura ADR numa pasta que o proprio pacote nao usa, e aceita qualquer valor em tarefa.adrs sem conferir arquivo nenhum. Onde: (1) validarRestricoesNoFechamento, em .mentor/scripts/restricoes.ts, chamada pelo task finalizar: lista os arquivos de `${c.docs}/adrs`, acha o arquivo da ADR com f.includes(adrId) e termina a condicao com '|| (tarefa.adrs ?? []).length > 0', entao qualquer texto em tarefa.adrs libera a trava, exista ou nao o arquivo e cite ou nao a restricao; (2) o bloco 'Restricoes fundadoras reavaliadas' da funcao processo, em .mentor/scripts/cmd-doctor.ts: lista join(c.docs, 'adrs') e casa o arquivo com arq.toUpperCase().includes(ID da ADR). As mensagens dos dois mandam criar a ADR em docs-mentor/adrs/. O caminho oficial do pacote e caminhos().adr, em .mentor/scripts/arquivos.ts, que vale docs-mentor/arquitetura/ADR, o mesmo que atualizarContagens (.mentor/scripts/vistas.ts) usa para contar ADRs. Caso medido em 15/09/26 no Eu Roteirizo, pacote 0.12.0: docs-mentor/adrs nao existe, docs-mentor/arquitetura/ADR existe, e nenhuma tarefa tem restricao com reconfirmada true, entao o defeito ainda nao disparou. Quando disparar: com as ADRs no caminho oficial, o doctor diz 'sem ADR vinculada' para sempre; com tarefa.adrs preenchido com qualquer valor, o finalizar aceita sem ADR nenhuma; e o casamento por substring faz 'ADR-1' valer pelo arquivo de qualquer ADR de 10 a 19. Visto na AUD-004-R04, no codigo do doctor que entrou pelo diff da TASK-CHORE-024. Sugestao: uma funcao so em restricoes.ts, usada pelo finalizar e pelo doctor, que procura em caminhos().adr o arquivo cujo nome tem o ID como palavra inteira (o numero da ADR seguido de um caractere que nao e digito), exige que esse arquivo exista e que o texto dele cite a chave da restricao (chaveDaRestricao), sem o atalho de tarefa.adrs nao vazio; as mensagens passam a citar caminhos().adr. Cenario: caminhos().adr com um unico arquivo, ADR-12-mapa.md; uma restricao que cita ADR-1, reconfirmada em duas tarefas concluidas; uma terceira tarefa que a reconfirma com tarefa.adrs ['ADR-1']. O task finalizar precisa recusar e o doctor precisa acusar a falta da ADR. Depois de criar ADR-1-rota.md citando a restricao na mesma pasta, os dois precisam aceitar.

- **15/09/26 07:54** · Com correcao local no .mentor/, o verificar fica REPROVADO o tempo todo, e um problema novo deixa de mudar o resultado. Onde: a funcao principal de .mentor/scripts/cmd-verificar.ts chama conferirManifesto (.mentor/scripts/cmd-pacote.ts), que transforma todo arquivo do .mentor/ diferente do manifesto num achado da familia referencia, e qualquer achado deixa o veredito REPROVADO. Caso medido em 15/09/26 no Eu Roteirizo, pacote 0.12.0, depois das TASK-CHORE-025 e TASK-CHORE-026: a saida inteira e 'REPROVADO. 1 achado(s): [referencia] .mentor/ (versao 0.12.0): 4 mudado(s): processos/entrega.md, scripts/cmd-doctor.ts, scripts/cmd-verificar.ts, scripts/vistas.ts'. Os 4 arquivos sao exatamente os das correcoes 1, 2 e 3 registradas em 'Corrigidas aqui', cada uma com a linha **Arquivos:**. Com isso, um link quebrado ou um marcador PREENCHER novo continua saindo REPROVADO, so com uma linha a mais na saida; e criterio de aceite no formato 'node mentor.mjs verificar aprova', usado na TASK-BG-023 (14/09/26) e na TASK-CHORE-024 (15/09/26), deixa de ser cumprivel enquanto houver correcao local. Visto na AUD-004-O09. A regra 6 do topo deste arquivo ja diz o que o verificar deveria fazer: arquivo na linha de divergencia sem entrada em 'Corrigidas aqui' e mudanca esquecida. Sugestao: o pacote adota como formato oficial a secao 'Corrigidas aqui' de docs-mentor/melhorias-do-pacote.md, com a linha **Arquivos:** de cada entrada, e o verificar le os caminhos entre crases que comecam com .mentor/ nessas linhas; arquivo divergente citado numa entrada sai como aviso, com o numero e o titulo da correcao, e nao reprova; arquivo divergente que nao aparece em nenhuma entrada continua achado e reprova. Nao precisa de hash nem de comando novo: usa o registro que a regra das correcoes locais ja exige. Cenario: projeto com dois arquivos do .mentor/ diferentes do manifesto, um citado na linha **Arquivos:** de uma entrada de 'Corrigidas aqui' e o outro nao; o verificar precisa sair REPROVADO citando so o segundo. Com os dois citados, precisa sair APROVADO, com um aviso por correcao.

- **16/09/26 23:53** · Narrativa de conclusao de tarefas renomeada para sufixo --estudo-humano.md, permitindo que o plano rico sirva de estudo para humanos e evitando consumo desnecessario de tokens por IAs em sessoes futuras.

- **17/09/26 11:21** · Trava no CLI: em projetos com PR obrigatorio (revisao_antes_do_merge), impedir mentor task iniciar a partir da main quando local estiver ahead de origin/main, e adicionar lembrete ao finalizar orientando push do ramo e abertura de PR em vez de merge local
