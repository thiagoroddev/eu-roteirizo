# TASK-REF-013 - Colapsar o card multi-rota gigante na aba Rotas

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** Normal (executada de imediato a pedido do humano)
- **Esforço-H/IA:** P/M
- **Data-hora origem:** 07/07/26 18:05 (print do humano — ROMANEIO DDP com 153 rotas ocupava a tela inteira)
- **Data-hora início:** 07/07/26 18:10
- **Data-hora conclusão:** 07/07/26 18:16
- **Dependências:** -
- **REQ/ADR/DT:** RF-46 (lista de salvos); RN-21/RN-23 preservadas

## Planejamento Aprovado

Card multi com **>6 rotas nasce colapsado** (header: tipo, contagem, nome, data, lixeira) + botão "Mostrar rotas (N)"/"Esconder rotas"; expandido, chips em área com `max-h-64` e rolagem interna. **Filtro da página passa a filtrar os CHIPS dentro do card** (antes só escondia cards): auto-expande enquanto filtra e mostra "X de N rotas". Cards ≤6 e single intactos. **Sem dependência nova** (Select/combobox Radix descartado: perderia os chips ricos `hasRoteiro`/AT; busca real exigiria cmdk — o filtro da página é a "busca do select").

## Execução

- 18:10: Plano aprovado ("prossiga, pra agora"); ciclo aberto; task movida de pendentes.
- 18:11: Labels `ROUTES_PAGE` (SHOW_ROUTES/HIDE_ROUTES/ROUTE_COUNT_FILTERED). `ManifestCard`: prop `filter`, `COLLAPSE_THRESHOLD = 6`, estado `expanded`, `matchingRoutes` (nome/AT, case-insensitive — mesma regra do filtro da página), `showChips = !collapsible || expanded || filtrando`, contagem dinâmica; toggle com `aria-expanded` (spread p/ o linter da IDE); área de chips com `max-h-64 overflow-y-auto`.
- 18:12: `RoutesPage` repassa `filter` ao card.
- 18:13: Testes `RoutesPage` +2 (fixture de 8 rotas: colapsado por padrão com toggle mostrar/esconder e card pequeno intacto; filtro estreita os chips + auto-expande + "1 de 8 rotas" + recolapsa ao limpar). 6 testes existentes passam sem edição (multi de 2 rotas fica abaixo do threshold).
- 18:14–18:16: Gates.

## Decisões Tomadas

- **Colapso + rolagem + filtro-nos-chips** no lugar do Select sugerido como alternativa: zero dependência nova, preserva os chips ricos (estado de roteiro RN-21 p/ RF-008 + código AT) e o filtro já existente da página faz o papel de busca.
- Threshold 6 (uma linha e meia de chips) — constante local, ajuste de 1 linha.
- Filtrar **sobrepõe** o colapso (o usuário está procurando uma rota); limpar o filtro recolapsa.

## Testes (gates rotulados)

- `npx tsc --noEmit`: **APROVADO**
- Suíte completa `npx vitest run`: **APROVADO** — 51 arquivos, **502/502** (+2)
- `npm run lint`: **APROVADO**
- `npm run build`: **APROVADO**
- **Smoke manual (humano): APROVADO em 07/07 ("top")**.

## Resultado

Aba Rotas escala: romaneios gigantes viram um card compacto de ~3 linhas; achar uma rota específica é digitar no filtro (que agora estreita os chips), sem varrer 153 botões.
