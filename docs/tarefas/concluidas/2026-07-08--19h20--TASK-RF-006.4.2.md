# TASK-RF-006.4.2 - Marcadores profissionais + parada selecionável + preview do raio

- **Status:** Concluído
- **Modo:** Standard (2ª rodada de feedback do humano 08/07 + revisão de UX aprovada)
- **Valor:** Crítico
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** M/G
- **Data-hora origem:** 08/07/26 18:10
- **Data-hora início:** 08/07/26 18:15
- **Data-hora conclusão:** 08/07/26 19:20
- **Dependências:** RF-006.4.1 ✅
- **REQ/ADR/DT:** spec §3/§4 revisadas (nota 08/07 2ª rodada no changelog); antecipação parcial da .6 (tap na parada + Editar/Desfazer)

## Planejamento Aprovado

U1 losango do início · U2 carro Tabler embutido sem cone/âncora central/Z abaixo dos endereços · U3 parada selecionável (painel do Original + Editar/Desfazer + estimativa a pé no cabeçalho) · U4 frase do órfão na seção certa · U5 sem Sequence (livres vazios; ordinal "1º/2º" em membros; quadrado mantém 1,2,3) · U6 preview do círculo do raio antes do Criar · M1–M10 (hierarquia, anel tracejado, ciano, chips, hint condicional, select pill, mini-marcador neon, ícones ±, addressLine, estimativa sem metros irrisórios).

## Execução

- 18:15: Ciclo aberto.
- 18:25: `markerSvg`: shape `diamond` (path próprio, stroke/glow herdam), prop `tip` (cone opcional), `ringStyle "dashed"` (dasharray 10 7 na borda selected), `glyph "car"` (paths Tabler car v3 outline embutidos, MIT — vence `number`; ink = numberInk). `markerIcon`: `anchor "center"` → iconAnchor [CX, CY_MID]. `ROTEIRO_ACCENT #00D1FF`.
- 18:35: `roteiroModels`: `selectedStopId` (anel no quadrado); membros do draft com `number` ordinal ("1º/2º"); candidato = selected+dashed+glow (funciona SEM draft — preview U6); adapter `pointToStopItemData(point, {ordinal?})` — `markerNumber` = ordinal ou "" (Sequence NUNCA mais); helpers `addressLineOf`/`stopPlaceSummaryFromPoints`/`walkEstimateLabel` (<20m → só minutos)/`orderedStopPoints`.
- 18:45: `StopItemRow` com prop `neon` (paleta do modo); `PanelMetricsRow` exportado do PanelTitle. RouteMap: `START_ICON_PROPS` (losango slate, ponta) ≠ `VEHICLE_ICON_PROPS` (carro, tip:false, anchor:center); overlay generalizado (rescale preserva as props/âncora de cada marcador — o carro não "anda" no zoom); `Z_VEHICLE=50000` (abaixo dos endereços); `Z_EXTERNAL_STOP=150000` + `EXTERNAL_STOP_SCALE_FACTOR=1.25` SÓ com `isExternal` (Original intocado); tracejada/círculo em `ROTEIRO_ACCENT`.
- 18:55: `RoteiroStopSection` novo (estrutura do Original: SECTION_STOP + Editar/Desfazer na linha do label + PanelTitle c/ bairros(CEPs)+chips+estimativa + SECTION_SELECTED + StopItemRow "1º" neon); `RoteiroPointSection` sem "Resumo da parada" (frase sob "Endereço selecionado"), select pill; `RoteiroDraftSection` com chips `PanelMetricsRow` + "Faltando" na edição + hint só com ≤1 endereço + PointRows com `addressLineOf`, prefixo ordinal nos escolhidos e botões ícone ± (aria completo) + stepper h-9.
- 19:05: MapPage: `selectedStopId` + contexto `stop-selected` (drafting > stop > point > start); tap no quadrado abre o painel (inerte durante o draft); `previewCandidateIds` puro + `radiusCircle` no preview do órfão; `handleEditStop` (REOPEN → edição com ordinais) / `handleDissolveStop` (DISSOLVE); card tap decidido pelo contexto ativo; sugestão com `addressLineOf`. Labels `ORDINAL`/`ROTEIRO_STOP`/`ESTIMATE_TIME_ONLY`. Spec: nota "08/07 2ª rodada" no changelog (losango; carro sem cone supera "mantém ponta fina" e motorbike→car; parada selecionável; preview; ordinais; ciano).
- 19:15: Testes: markerSvg +4 (diamond/tip/dasharray/glyph), markerIcon +1 (anchor central), roteiroModels ±8 (ordinais, preview sem draft, selectedStopId, adapter sem Sequence, 4 helpers), RoteiroStopSection novo (2), Point/DraftSection revisados (seção movida; chips/remaining/hint; ordinal + ícones ±), MapPage ±3+1 (preview na tela 8 + tap na parada → Editar/Desfazer ponta a ponta), RouteMap (âncora = marker do veículo).

## Decisões Tomadas

- **Losango na paleta `vehicle` slate** — início e veículo são infraestrutura da rota; a FORMA distingue (zero paleta nova).
- **`Z_VEHICLE` abaixo dos endereços** — a motivação do sem-cone é o carro nunca cobrir um endereço; o deslocamento pra rua + z baixo garantem.
- **Boosts de escala/z da parada firmada SÓ sob `isExternal`** — Original pixel-idêntico (suíte é a rede).
- **Editar parada = REOPEN_STOP** (a edição da .6 já funciona ponta a ponta para o caso reabrir→salvar; a lista/expansão da parada no mapa continua sendo a .6).
- Tabler `car` em vez do `motorbike` sugerido (mesma família MIT); Modo de Transporte configurável = RF-007.

## Testes (gates rotulados)

- `npx tsc --noEmit`: **APROVADO**
- Suíte completa `npx vitest run`: **APROVADO** — 62 arquivos, **598/598** (583 + 15 líquidos; ~10 revisados por definição). Nota: 1 flake de carga alheio à tarefa (`useRouteUploader` >10MB) numa rodada intermediária — verde isolado e na re-rodada completa; anotar se reincidir (candidato a TASK-TEST).
- `npm run lint`: **APROVADO**
- `npm run build`: **APROVADO**
- ⚠️ **Smoke manual (humano):** (1) início = LOSANGO slate; (2) tocar órfão → círculo ciano tracejado + candidatos com anel TRACEJADO já no preview + painel "Endereço selecionado" com a frase no lugar certo; (3) Criar parada → edição com membros "1º/2º" no mapa e na lista, carro na rua (não cobre endereços; zoom in/out — o carro não pode "andar"); (4) salvar → quadrado 25% maior numerado; TOCAR o quadrado → painel do Original (Parada N — bairros (CEP) + chips com "~min · m a pé") + card "1º" expansível + **Editar parada** (volta à edição) e **Desfazer parada** (libera endereços); (5) nenhum Sequence visível no modo; (6) hint do toque some com o 2º endereço; select pill; ícones ±; (7) Original pixel-idêntico; (8) **calibrar**: centro ótico do carro, tracejado do anel em zoom baixo, tom do ciano.

## Resultado

O mapa do Meu roteiro ganhou hierarquia e identidade: losango de largada, carro na rua (sem cone, sem cobrir endereços), paradas maiores e selecionáveis com painel idêntico ao Original (+ Editar/Desfazer reais), candidatos com anel tracejado dentro do círculo ciano que agora aparece ANTES de criar, ordinais "1º/2º" no lugar dos Sequence, e o pacote de consistência (chips, pill, ±, addressLine, estimativa honesta). Próxima: **RF-008 antecipada**.
