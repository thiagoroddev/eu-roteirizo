# TASK-REF-003 - Usar hasValidFileExtension no hook (remover duplicação / código morto)

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Desejável
- **Urgência:** Normal
- **Esforço-H/IA:** P/P
- **Data-hora origem:** 22/06/26 11:05
- **Data-hora início:** 22/06/26 14:23
- **Data-hora conclusão:** 22/06/26 14:37
- **Dependências:** -
- **REQ/ADR/DT:** REV-001-A10
- **Observações:** Achado REV-001-A10 (código morto + duplicação).

## Planejamento Aprovado

### Problema (evidência)
- `src/utils/validators.ts:44` define e exporta `hasValidFileExtension(fileName, validExtensions)` (lowercase do nome + `endsWith`), com teste em `validators.test.ts`.
- `src/hooks/useRouteUploader.ts:78` reimplementava a mesma lógica inline: `FILE_CONFIG.ACCEPTED_EXTENSIONS.some((ext) => file.name.toLowerCase().endsWith(ext))`.
- Comportamento idêntico → função era "código morto em produção" (só exercitada por teste) + duplicação.

### Decisão: USAR (não remover)
Usar a função no hook elimina a duplicação E mantém um helper testado e reutilizável, agora vivo em produção.

### O que muda
- `src/hooks/useRouteUploader.ts`: importar `hasValidFileExtension` de `../utils/validators` e trocar a linha inline por `const hasValidExtension = hasValidFileExtension(file.name, FILE_CONFIG.ACCEPTED_EXTENSIONS);`.

### Critérios de aceite
- `useRouteUploader` usa `hasValidFileExtension`; sem reimplementação inline.
- Comportamento idêntico; `vitest` 224/224; `tsc -b` exit 0; `lint` exit 0.

## Execução
- 14:23: Movida de pendentes para em-andamento; plano proposto.
- 14:35: Plano aprovado pelo humano ("sim").
- 14:36: `useRouteUploader.ts` — import de `hasValidFileExtension` + substituição da checagem inline pela chamada da função.
- 14:37: Gates → `vitest` 224/224; `tsc -b` exit 0; `lint` exit 0.

## Decisões Tomadas
- **Usar a função em vez de removê-la:** mantém o helper testado e reutilizável e elimina a duplicação — preferível a apagar código que já tem cobertura de teste.

## O Que NÃO Foi Feito (e Por Quê)
- Nenhum item pendente. (A alternativa de remover a função + teste foi descartada por ser inferior — perderia um util testado e deixaria a lógica inline.)

## Testes
- 1º (baseline) `vitest`: 224/224.
- Último `vitest`: 224/224 — `useRouteUploader.test.ts` (hook) e `validators.test.ts` (função) já cobrem o caminho; comportamento inalterado.
- `tsc -b`: exit 0. `lint`: exit 0.
- Sem testes novos: comportamento idêntico, apenas substituição da implementação inline pela função já testada.

## Revisão
Auto-revisão (Standard): mudança mínima (1 arquivo); elimina duplicação; usa helper testado; comportamento preservado; gates verdes. OK.

## Tarefas Geradas pela Revisão
- Nenhuma.

## Requisitos Gerados pela Revisão
- Nenhum.

## ADRs Geradas pela Revisão
- Nenhuma.

## Aprendizados Para o Projeto
- "Código morto" que tem teste e um equivalente inline em produção geralmente é sinal de duplicação: a melhor resolução costuma ser **usar** o util (não removê-lo), unificando a lógica num ponto já testado.
