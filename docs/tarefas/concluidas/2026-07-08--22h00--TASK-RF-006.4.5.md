# TASK-RF-006.4.5 - Linha do rótulo da "Parada sugerida": distância com ícone de carro + Criar parada à direita

- **Status:** Concluído
- **Modo:** Standard (4ª rodada — ordens diretas do humano 08/07)
- **Valor:** Alto
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** P/P
- **Data-hora origem:** 08/07/26 21:50
- **Data-hora início:** 08/07/26 21:55
- **Data-hora conclusão:** 08/07/26 22:00
- **Dependências:** RF-006.4.4 ✅
- **REQ/ADR/DT:** fluxo §8 (nota ".4.5" anexada ao changelog)

## Planejamento Aprovado

Lista imperativa do humano (o próprio pedido é o plano; itens 1 e 3 do pedido descrevem o mesmo elemento — a distância ao lado do rótulo "Parada sugerida"): (1) distância da última parada (ou início) até a sugerida = **"Distância até aqui: 1,2 km" + ÍCONE de veículo, ao lado do rótulo**; (2) remover "O raio engloba N candidatos" da seção; (3) remover a linha "Do início até aqui: …"; (4) **"Criar parada" no canto direito da linha do rótulo**. "Incorporar em outra parada" permanece abaixo (não citado no pedido).

## Execução

- 21:55: `PanelSection` ganhou o slot `meta` (info inline ao lado do rótulo; caminho sem meta idêntico ao anterior — Original intocado). Labels: `DISTANCE_TO_HERE`/`VEHICLE_QUALIFIER` substituem `VEHICLE_FROM_STOP`/`VEHICLE_FROM_START`.
- 21:57: `RoteiroPointSection`: linha do rótulo = distância (truncável) + ícone `Car` (lucide) `aria-hidden` + sr-only "de veículo" (regra da distância qualificada vale p/ leitores de tela) + CTA "Criar parada" no slot actions; banner de candidatos e parágrafo da distância removidos; "Incorporar em outra parada" abaixo do resumo. MapPage: label única independente da origem (última parada ou início); prop `candidateCount` removida.
- 21:59: Testes revisados: RoteiroPointSection (linha do rótulo contém distância + qualificador sr + CTA; banner ausente; sufixo "(linha reta)"), MapPage tela 8 (sem banner; regex "Distância até aqui: … (linha reta)").

## Decisões Tomadas

- **Ícone qualifica, sr-only fala** — a regra de ouro "distância sempre qualificada" migrou do texto p/ o ícone de carro com `sr-only` "de veículo"; o "(linha reta)" continua textual (honestidade do fallback).
- **Contagem do raio não sumiu do produto** — segue no card do raio durante a edição; só saiu do preview (ordem do humano).
- **Origem da perna (última parada × início) deixou de ser nomeada** — label única "Distância até aqui" (o humano removeu o "Do início até aqui").

## Testes (gates rotulados)

- `npx tsc --noEmit`: **APROVADO**
- Suíte completa `npx vitest run`: **APROVADO** — 63 arquivos, **603/603**.
- `npm run lint`: **APROVADO**
- `npm run build`: **APROVADO**
- ⚠️ **Smoke manual (humano):** (1) linha do rótulo: "Parada sugerida · Distância até aqui: X 🚗 … [Criar parada]" — conferir truncamento em telas estreitas (endereços longos × botão); (2) sem "O raio engloba…" e sem "Do início até aqui…" na seção; (3) sem grafo, sufixo "(linha reta)"; (4) "Incorporar em outra parada" segue abaixo, com o select sob demanda.

## Resultado

A seção "Parada sugerida" ficou em uma linha de rótulo densa e útil — distância de veículo qualificada pelo ícone de carro + CTA à direita — com o resumo (título + chips) logo abaixo, sem as duas linhas removidas. Próxima: **RF-008 antecipada**.
