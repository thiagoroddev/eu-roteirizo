# TASK-RF-022.3 - Aba Rotas: lista de salvos (cards tipados + chips por rota/AT)

- **Status:** Concluído
- **Modo:** Standard (subtarefa do épico Strict RF-022)
- **Valor:** Crítico
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** M/G
- **Data-hora origem:** 05/07/26 04:06
- **Data-hora início:** 05/07/26 19:32
- **Data-hora conclusão:** 05/07/26 19:42
- **Dependências:** TASK-RF-022.1 ✅, TASK-RF-022.2 ✅, TASK-RF-011 ✅
- **REQ/ADR/DT:** RF-44 (parte lista → com a HOME da .2, RF-44 fica 🟡/quase ✅), RF-46 (**reabrir sem reenviar — completo**), RN-23 (**completa**: redirect + seleção), RN-21 (contrato `hasRoteiro` no chip); fluxo §15.2; `2-ROTAS.png`, `3-SELECIONAR-rotas-apos-envio.png`
- **Observações:** Destino do chip na fase 1 = visualizador atual via deep link; a .4 muda só o destino para o Sumário de foco.

## Planejamento Aprovado

**Decisão de transição (aprovada):** tocar num chip navega para **`/?romaneio={id}&rota={nome}`**; o `RouteViewer` lê os parâmetros, carrega o manifest (bytes → `File` → `processExcelFile`) e pré-seleciona a rota — o usuário cai no Sumário inline atual. Na .4, só o destino muda. Bônus: URL com estado = deep link real (ADR-003).

**O que muda:** hook `loadManifest(id)` (mesmo pipeline do upload, sem re-salvar) + tipo; `RoutesPage` real (lista, busca sobre metas, vazio, `?sel=`); `components/manifests/` (`ManifestCard` com Dialog de confirmação, `RouteChip` com `hasRoteiro?`); `RouteViewer` (deep link com guarda + redirect RN-23); labels `ROUTES_PAGE` ampliados. **Decisão registrada:** busca filtra as **metas** (o `at` já vive no `ManifestMeta`) — dispensa o `useRouteSearch` citado no texto do épico, que exigiria carregar as rows de todos os arquivos.

**Testes:** `RoutesPage.test.tsx` novo; hook `loadManifest`; `RouteViewer.test.tsx` em MemoryRouter + navegação.
**Gates:** suíte completa + `tsc` + `lint` + `npm run build`, rotulados.

## Execução

- 05/07/26 19:32: Plano aprovado no chat. Task iniciada (baseline 393/393 da .2).
- 19:33–19:37: Implementado: labels `ROUTES_PAGE` (título, busca, vazio, tipos, apagar/confirmação) + `MANIFEST_NOT_FOUND`; `loadManifest` no hook + assinatura no tipo; `RouteChip` (ícone de estado sem/com roteiro — fase 1 sempre "sem") e `ManifestCard` (Badge por tipo, data pt-BR, lixeira + `Dialog` de confirmação, realce `?sel=`); `RoutesPage` real (load ao montar com guarda de unmount, filtro memoizado por nome/AT, navegação do chip); `RouteViewer` (2 effects: deep link com `loadedManifestRef` + redirect de duplicado com `replace`); `currentRows` ganhou `?? []` (deep link para rota inexistente não quebra).
- 19:38: Testes: +3 no hook (reabrir com File fiel/sem re-save, id inexistente, erro de processamento); `RouteViewer.test` migrado para `MemoryRouter` (helper `renderViewer`) + 2 testes de navegação; `RoutesPage.test.tsx` novo (7 testes: cards/chips, vazio, filtro por AT, navegação do chip via probe de location, destaque `?sel=`, apagar com confirmação, cancelar). `AppShell.test` atualizado (o label `COMING_SOON` morreu com o stub → asserta o título real).
- 19:40–19:42: Gates verdes.

## Decisões Tomadas

- **Deep link `/?romaneio&rota` como destino da fase 1:** dá RF-46 completo já (reabrir sem reenviar) e a .4 troca apenas o destino; URL recarregável/compartilhável (justifica o BrowserRouter da ADR-003).
- **Busca sobre metas, não `useRouteSearch`:** o `at` foi desenhado dentro do `ManifestMeta` na .1 exatamente para isso; carregar rows de todos os arquivos salvos só para buscar seria o caminho errado. Divergência do texto do épico registrada.
- **Confirmação de exclusão via `Dialog` existente** (ADR-004: primitivo complexo → Radix, já instalado): destrutivo não pode ser um clique só.
- **Guarda `loadedManifestRef`:** o effect de deep link roda uma vez por id (StrictMode/re-render não recarregam).

## O Que NÃO Foi Feito (e Por Quê)

- **Destino = Sumário de foco:** RF-022.4.
- **Chips com estado "com roteiro" real:** RF-008 (persistência de Roteiros); o contrato (`hasRoteiro?`) já existe.
- **Card "Roteiro Exportado":** RF-013.

## Testes

- Baseline: 393/393. Novos: +12 (3 hook, 2 RouteViewer navegação, 7 RoutesPage).
- Modificados: `RouteViewer.test` (MemoryRouter — mecânico), `AppShell.test` (1 asserção, label substituído).
- **Gates finais (rotulados):**
  - `npx vitest run src/__tests__` → **APROVADO** (**405/405**, 41 arquivos)
  - `npx tsc --noEmit` → **APROVADO** · `npm run lint` → **APROVADO** · `npm run build` → **APROVADO** (PWA ok)

## Revisão

Auto-revisão (módulo 21): componentes de domínio em `components/manifests/` (ADR-004); texto 100% `UI_LABELS`; sem `any`; chips com `aria-label`; efeitos com guarda e cleanup; chave estável (`manifest.id`/`route.name`) nas listas; nenhuma dependência nova. Sem achados.

## Tarefas Geradas pela Revisão

- -

## Requisitos Gerados pela Revisão

- **RF-46 → ✅** (salvar/listar/reabrir/apagar romaneios completos); **RN-23 → ✅** (dedup + aviso + redirect com seleção). Atualizar `docs/requisitos/` (feito nesta task).

## ADRs Geradas pela Revisão

- -

## Aprendizados Para o Projeto

- O investimento da .1 em metadados ricos (`routes[].at`) pagou na .3: busca por AT sem carregar nenhum arquivo. Desenhar a meta pensando na tela seguinte evita retrabalho.
