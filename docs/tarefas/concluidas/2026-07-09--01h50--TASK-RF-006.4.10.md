# TASK-RF-006.4.10 - Modo Original: 1 clique mantém agrupado/foca, 2 cliques expande, clicar-fora reagrupa (fatia FINAL do epic .4.6)

- **Status:** Concluído
- **Modo:** Standard (epic .4.6, fatia FINAL — plano aprovado; **muda o contrato de interação do RF-023**)
- **Valor:** Importante
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** G/G
- **Data-hora origem:** 09/07/26 01:45
- **Data-hora início:** 09/07/26 01:50
- **Data-hora conclusão:** 09/07/26 01:55
- **Dependências:** RF-006.4.8 ✅ (infra dblclick) · RF-006.4.9 ✅
- **REQ/ADR/DT:** **muda o contrato de interação do RF-023** (antes: 1 clique expande); unifica o modelo dos dois modos; plano `como-resolver-o-shell-graceful-gadget.md`

## Planejamento Aprovado

Unifica o modelo de interação (vale p/ o Original agora, o roteiro já veio nas .4.8/.4.9): **1 clique** numa parada → **foca** (fitBounds) mas **mantém agrupado** (quadrado, ênfase) + painel resumo; **2 cliques** no quadrado (ou **"Ver lista completa"**) → **expande** em endereços individuais + painel lista; **clicar fora** → **reagrupa** mantendo o foco na parada.

## Execução

- 01:50: Ciclo aberto.
- 01:52: `markerModels` — `focusInteraction` (expandedStopKey null + 1º endereço), `expandInteraction` (expande), `nextInteraction` passa a FOCAR no clique de stop (address segue selecionando), `regroupInteraction(current)` (colapsa mantendo foco), `collapseInteraction` (reset total) segue p/ troca de modo; `computeMarkerModels` dá `selected`/`emphasis` ao quadrado FOCADO-colapsado (derivado de `selectedAddressKey` com `expandedStopKey` null); helper `focusedStopIndex`.
- 01:53: RouteMap — `interactionRef` (fresco); clique INTERNO agora usa o timer (foca no fire) + `dblclick` (stop → expande / address → seleciona); empty-map → `regroupInteraction(interactionRef.current)`; fitBounds ganhou o ramo "focado-colapsado" (fit na parada focada, DEFAULT; `selectedAddressKey` entrou nas deps).
- 01:54: MapPage — `handleStepStop` → `focusInteraction` (agrupado); "Ver lista completa" (`handleShowList`) → `expandInteraction` (mapa desagrupa junto) / "Esconder lista" (`handleHideList`) → `regroupInteraction`; `handleCardTap` não força expandir (mantém `expandedStopKey`); Escape/arrasto saindo da lista → regroup; **`applyInteraction` passou a mover a memória do painel pela parada FOCADA** (`selectedAddressKey`), não só pela expandida — senão o stepper/clique não moviam o painel com `expandedStopKey` null.
- 01:55: Testes — markerModels (nextInteraction=foca; focus/expand/regroup; ênfase no quadrado focado); RouteMap (clique adiado=foca via fake timers; novo dblclick=expande); MapPage (card tap não expande o mapa; stepper foca [mapa agrupado, painel segue]; "Ver lista completa" expande / "Esconder lista" reagrupa); integração (clique real adiado=foca, fitBounds DEFAULT). Prettier no import multi-linha.

## Decisões Tomadas

- **Timer também no clique interno** (Original) — mesma razão da .4.8 (o 1º clique de um duplo recria os marcadores); o single-click fica ~220ms mais lento (calibrar no smoke; alternativa futura: atualizar ícone in-place em vez de recriar).
- **`applyInteraction` move o painel pela parada focada** — necessário porque o foco tem `expandedStopKey` null; sem isso o stepper e o clique-simples não moviam a memória do painel. Colapso total (ambos null, troca de modo) continua preservando a memória.
- **Card tap não expande o mapa** — o mapa só desagrupa por duplo-clique/"Ver lista completa"; o card só abre o detalhe no painel.
- **"Ver no mapa" (lista) mantido** — expande p/ destacar o endereço específico (ação explícita "me mostra este endereço").
- **doubleClickZoom off** (desde a .4.8) cobre os dois modos.

## Testes (gates rotulados)

- `npx tsc --noEmit`: **APROVADO**
- Suíte completa `npx vitest run`: **APROVADO** — 63 arquivos, **619/619** (613 + 6 líquidos; ~6 do Original revisados por definição — mudança de contrato RF-023).
- `npm run lint`: **APROVADO**
- `npm run build`: **APROVADO**
- ⚠️ **Smoke manual (humano) — CUIDADO, muda o Original (RF-023 travado):** (1) **1 clique** numa parada (Original) → **foca/centraliza mas mantém AGRUPADO** (um quadrado com ênfase) + painel resumo; **NÃO expande mais** no 1º clique; (2) **2 cliques** no quadrado (ou "Ver lista completa") → **expande** em endereços individuais numerados + painel lista; conferir que o 2º clique NÃO dá zoom e que o duplo-clique funciona (220ms); (3) **clicar fora** (mapa vazio) → **reagrupa** mantendo a parada focada (painel não esvazia); (4) stepper ‹ › foca agrupado; (5) "Esconder lista"/Escape/arrasto → reagrupa; (6) **regressões a vigiar**: snap do painel, "painel nunca auto-eleva por seleção no mapa", memória `panelStopKey`, o 1º endereço selecionado ao abrir a parada; (7) mobile: duplo-toque sensível → "Ver lista completa" é o caminho garantido.

## Resultado — EPIC .4.6 COMPLETO

O modelo de interação agora é **um só nos dois modos**: 1 clique foca/agrupa, 2 cliques (ou "Ver lista completa") desagrupa, clicar-fora reagrupa mantendo o foco. Fecha o epic .4.6 (criar-comita → painel da parada → desagrupar no mapa → editar sem toggle → **Original unificado**). Pendências conhecidas: calibrar a janela de 220ms e o zoom do foco no smoke; a re-projeção da âncora (decisão C) e o geocoding da âncora (TASK-RF-006.9) seguem para depois.
