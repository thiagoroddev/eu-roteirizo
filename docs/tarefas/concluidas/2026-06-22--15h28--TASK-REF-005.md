# TASK-REF-005 - Limpezas da REV: guard clause duplicada, typo `colums`, setTimeout sem cleanup

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Desejável
- **Urgência:** Normal
- **Esforço-H/IA:** P/P
- **Data-hora origem:** 22/06/26 15:26
- **Data-hora início:** 22/06/26 15:26
- **Data-hora conclusão:** 22/06/26 15:28
- **Dependências:** -
- **REQ/ADR/DT:** REV-001-A08, REV-001-A12
- **Observações:** Agrupa os achados 🟢 A08 e A12 da REV-001. Tratado como Standard (não Light) porque o A12b altera lógica (cleanup de timeout), fora da lista fechada de Light (§4.1).

## Planejamento Aprovado

### A08 — guard clause duplicada (RouteSimpleTable)
`src/components/RouteSimpleTable.tsx:133-137` tinha o mesmo `if (!rows || rows.length === 0) return null;` duas vezes (2ª inalcançável). Remover a 2ª ocorrência.

### A12a — typo `colums` (RouteTable)
`src/components/RouteTable.tsx`: renomear a variável local `colums` → `columns` (declaração `:24` + usos `:40`, `:50`).

### A12b — setTimeout sem cleanup (RouteMap)
`src/components/RouteMap.tsx:191-193`: o `setTimeout(() => map.invalidateSize(), 100)` no effect `[rows]` não era limpo. Capturar o id e limpar no cleanup do effect (`return () => clearTimeout(...)`).

### Critérios de aceite
- Sem guard clause duplicada; `columns` sem typo; timeout do RouteMap limpo no cleanup.
- `vitest` 235/235; `tsc -b` exit 0; `lint` exit 0.

## Execução
- 15:26: Criada e movida para em-andamento (origem REV-001-A08/A12); plano proposto.
- 15:27: Plano aprovado pelo humano ("sim").
- 15:27: A08 — removida a 2ª guard clause em `RouteSimpleTable.tsx`.
- 15:27: A12a — `colums` → `columns` em `RouteTable.tsx` (declaração + 2 usos).
- 15:27: A12b — `RouteMap.tsx`: timeout capturado em `resizeTimeout` + `return () => clearTimeout(resizeTimeout)` no effect `[rows]`.
- 15:28: Gates → `vitest` 235/235; `tsc -b` exit 0; `lint` exit 0.

## Decisões Tomadas
- **Agrupar A08 + A12 numa só tarefa:** são três limpezas triviais da mesma REV; um ciclo só mantém a cerimônia proporcional sem perder rastreabilidade (a tarefa cita ambos os achados).
- **Standard em vez de Light:** o A12b muda lógica (cleanup), o que sai da lista fechada de Light do núcleo (§4.1).

## O Que NÃO Foi Feito (e Por Quê)
- Nada pendente nestes três pontos.

## Testes
- 1º (baseline) `vitest`: 235/235.
- Último `vitest`: 235/235. `tsc -b`: exit 0. `lint`: exit 0.
- Sem testes novos: A08/A12a não mudam comportamento; A12b é cleanup de timeout não observável pelos testes (que mockam Leaflet). A robustez do unmount do RouteMap já é coberta por "removes the map instance on unmount" / "cleans up event listeners".

## Revisão
Auto-revisão (Standard): três correções mínimas e isoladas; removem duplicação/typo e um leak de timeout; gates verdes. OK.

## Tarefas Geradas pela Revisão
- Nenhuma.

## Requisitos Gerados pela Revisão
- Nenhum.

## ADRs Geradas pela Revisão
- Nenhuma.

## Aprendizados Para o Projeto
- `setTimeout` dentro de `useEffect` deve sempre ter `clearTimeout` no cleanup para evitar acúmulo quando as dependências mudam rápido ou o componente desmonta antes de disparar.
