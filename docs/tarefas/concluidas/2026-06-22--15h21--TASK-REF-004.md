# TASK-REF-004 - Usar UI_LABELS.ROUTE_SELECTOR no RouteSelector (strings hardcoded)

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Desejável
- **Urgência:** Normal
- **Esforço-H/IA:** P/P
- **Data-hora origem:** 22/06/26 15:01
- **Data-hora início:** 22/06/26 15:08
- **Data-hora conclusão:** 22/06/26 15:21
- **Dependências:** -
- **REQ/ADR/DT:** REV-001-A05, ADR-001
- **Observações:** Gap descoberto na TASK-TEST-001 — strings que escaparam da TASK-REF-001. Refactor puro (valores idênticos).

## Planejamento Aprovado

### Problema (evidência)
`src/components/RouteSelector.tsx:66-67` (em `getDisplayText`) hardcodava dois textos de UI que já existem em `UI_LABELS.ROUTE_SELECTOR`:
- `"-- Aguardando arquivo --"` ↔ `UI_LABELS.ROUTE_SELECTOR.WAITING_FILE`
- `` `-- Escolha uma das ${totalRoutes} rotas --` `` ↔ `UI_LABELS.ROUTE_SELECTOR.CHOOSE_ROUTE(totalRoutes)`
(`UI_LABELS` já estava importado — usado em `COMMON.NO_DATA`.)

### O que muda
- `src/components/RouteSelector.tsx`:
  - `return "-- Aguardando arquivo --";` → `return UI_LABELS.ROUTE_SELECTOR.WAITING_FILE;`
  - `return \`-- Escolha uma das ${totalRoutes} rotas --\`;` → `return UI_LABELS.ROUTE_SELECTOR.CHOOSE_ROUTE(totalRoutes);`

### Critérios de aceite
- `RouteSelector` não hardcoda mais esses textos; valores idênticos → sem mudança visível.
- `vitest` 235/235; `tsc -b` exit 0; `lint` exit 0.

## Execução
- 15:08: Movida de pendentes para em-andamento; plano proposto.
- 15:19: Plano aprovado pelo humano ("sim").
- 15:20: `RouteSelector.tsx` — duas substituições por `UI_LABELS.ROUTE_SELECTOR.WAITING_FILE` e `.CHOOSE_ROUTE(totalRoutes)`.
- 15:21: Gates → `vitest` 235/235; `tsc -b` exit 0; `lint` exit 0.

## Decisões Tomadas
- Nenhuma decisão não-óbvia: substituição direta por labels de valor idêntico já existentes.

## O Que NÃO Foi Feito (e Por Quê)
- Nada pendente. Conclui o alinhamento do RouteSelector à ADR-001 (UI centralizada), fechando o gap deixado pela TASK-REF-001.

## Testes
- 1º (baseline) `vitest`: 235/235.
- Último `vitest`: 235/235 — `RouteSelector.test.tsx` e `RouteViewer.test.tsx` já consultavam por `UI_LABELS.ROUTE_SELECTOR`, então passaram a refletir a fonte única sem mudança.
- `tsc -b`: exit 0. `lint`: exit 0.
- Sem testes novos: refactor de valor idêntico já coberto pelos testes existentes.

## Revisão
Auto-revisão (Standard): mudança mínima (1 arquivo, 2 linhas); remove duplicação de literais; cumpre ADR-001; comportamento idêntico; gates verdes. OK.

## Tarefas Geradas pela Revisão
- Nenhuma.

## Requisitos Gerados pela Revisão
- Nenhum.

## ADRs Geradas pela Revisão
- Nenhuma. (Implementa a ADR-001.)

## Aprendizados Para o Projeto
- Inventários de "strings hardcoded" podem deixar passar ocorrências dentro de funções (ex.: `getDisplayText`) — escrever testes que consultam por `UI_LABELS` (e não por literais) é uma rede de segurança eficaz para flagrar esses casos.
