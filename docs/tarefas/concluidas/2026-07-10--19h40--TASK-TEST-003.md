# TASK-TEST-003 - Testar o zoom/enquadramento REAL do mapa (composição MapPage → RouteMap)

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** Normal
- **Esforço-H/IA:** P/M
- **Data-hora origem:** 09/07/26 20:35
- **Data-hora início:** 10/07/26 19:20
- **Data-hora conclusão:** 10/07/26 19:40
- **Dependências:** RF-006.4.19/.4.20/.4.21/.4.22/.4.24 ✅ (os comportamentos que os testes pinam)
- **REQ/ADR/DT:** RF-006.4.19/.4.20/.4.21; lição registrada na RF-006.4.20 ("suíte verde ≠ comportamento coberto")

## Planejamento Aprovado

**Contexto:** dois defeitos de zoom (.4.19 — offset aplicado a TODO fitBounds quebrou o desagrupar; .4.20 — zoom por contexto) atravessaram a suíte verde. Motivo estrutural, confirmado por exploração: `MapPage.test.tsx` assere só a **prop** `focusMaxZoom`/`focusBounds` contra um **stub `<div>`**; `RouteMap.test.tsx` assere a opção `maxZoom` do `fitBounds` mas com **props sintéticas** (e na .4.20 tinha **codificado o defeito**); `MapPage.integration.test.tsx` (composição real, Leaflet mockado) só cobre modo Original, 1 parada, e só assere `paddingBottomRight` — nunca `maxZoom`. Ninguém observa o `maxZoom` que o `fitBounds` REAL recebe quando a cadeia de precedência do MapPage (`roteiroFocus`/`draftFrame`) flui até o efeito de fit do RouteMap. A TEST-003 fecha esse buraco **antes** da RF-006.8, que mexe na altura do painel → `bottomObstructionPx` → `fitBounds`. Nota de técnica: "Leaflet espionado" = módulo `leaflet` inteiramente mockado (jsdom não tem canvas); o próprio `fitBounds` é `vi.fn()` e serve de spy — o teste assere a **opção `maxZoom` passada**, que é exatamente onde os dois defeitos viveram.

**O que muda (um único arquivo de teste; nenhum código de produto):** `src/__tests__/pages/MapPage.integration.test.tsx` — estender:
1. **Upgrade do mock do Leaflet** (hoje `marker: vi.fn(() => markerMethods)` compartilhado — impossível clicar num marcador específico): `marker(latlng)` vira **factory** (instância própria com `on` próprio, registrada em `createdMarkers` com `{latlng, handlers}`); `latLngBounds` captura os pontos recebidos; `mapMethods.fitBounds` continua o spy compartilhado; os 4 testes existentes se adaptam (`clickLastMarker` → registro).
2. **Mocks novos** (replicando o que `MapPage.test.tsx` já tem): `routeStorage` (hoisted, vazio), `useRoadGraph` (graph `null` → fallback haversine), geolocation.
3. **Helpers:** `lastFitBounds()` (última chamada — `mock.calls.at(-1)` ou `vi.clearAllMocks()` antes do gesto), `tapMarkerNear(lat,lng)` (single-tap deferido 300ms, fake timers), `dblTapMarkerNear`, `tapMap(lat,lng)`, `enterRoteiro()` (rota `?modo=roteiro`, início por tap + botão real via `UI_LABELS`).
4. **Fixture multi-ponto** (geometria do `rowsThreePoints`: p2 ~17m dentro do raio, p3 ~556m fora).

**Cenários (asserção = `maxZoom` da última chamada real de `fitBounds`):**

| # | Cenário | Regressão de | Esperado |
|---|---|---|---|
| 1 | Roteiro: tap em parada firmada (agrupada) | .4.19/.4.20 | `FOCUS_MAX_ZOOM` (17) |
| 2 | Roteiro: parada expandida (double-tap) | .4.20 | `MAP_CONFIG.ZOOM.MAX` (19) |
| 3 | Roteiro: tap em endereço livre (órfão) | .4.21/.4.22 | `ADDRESS_MAX_ZOOM` (18) |
| 4 | Roteiro: tap em membro de parada | .4.21 | `ADDRESS_MAX_ZOOM` (18) |
| 5 | Roteiro: nada selecionado → rota inteira | .4.20 | `ZOOM.DEFAULT` (16), sem `focusBounds` |
| 6 | Roteiro: "Editar parada" mantém o frame | .4.24 | `FOCUS_MAX_ZOOM` (17) + mesma contagem de pontos no `latLngBounds` |
| 7 | Roteiro: obstrução do painel no padding | rede p/ RF-006.8 | `paddingBottomRight: [40, 40 + PANEL_COLLAPSED_PX]` |
| 8 | Original: marcador clicado (focado agrupado) | .4.19 | `FOCUS_MAX_ZOOM` (17) |
| 9 | Original: parada expandida | .4.19/.4.20 | `MAP_CONFIG.ZOOM.MAX` (19) |

(~8–9 testes novos; 3/4 podem fundir se o setup ficar idêntico.) Docstring do arquivo atualizada: passa a ser **o contrato de zoom real** da composição.

**Critérios de aceite:** cada linha da tabela vira asserção sobre a chamada **real** de `fitBounds` do RouteMap real montado pelo MapPage real; os 4 testes existentes continuam verdes; **prova de valor** — reintroduzir localmente o defeito da .4.19 deve derrubar os cenários 2/9, e o da .4.20 o 5 (mutação manual, sem commitar, registrada no log); gates rotulados (`tsc --noEmit` · `eslint src/` · `vitest run` suíte inteira · `build`).

**Impacto:** só `src/__tests__/`. **Riscos:** (1) upgrade do mock quebrar os 4 testes existentes → adaptar na mesma edição; (2) flakiness dos taps deferidos → fake timers, padrão já validado; (3) testes pinam os knobs atuais (16/17/18/19) — intencional, são o contrato (⚙️ MANUAL KNOB: recalibrar knob = atualizar o teste junto). **Dependências novas:** nenhuma.

## Execução

- 19:20: Plano aprovado. Baseline **652/652**.
- 19:30: Arquivo estendido: mock do Leaflet com `marker` factory (registro `createdMarkers` com latlng + handlers por instância), `latLngBounds` ecoando os pontos, mocks de `routeStorage`/`useRoadGraph`, helpers (`tapMarkerAt`/`dblTapMarkerAt`/`tapMap`/`lastFitBounds`/`enterRoteiro`/`createFirstStop`), 8 cenários novos. Geolocation ficou de fora (decisão): o início por tap não usa GPS — mock morto seria ruído.
- 19:32: 1ª rodada — 5 falhas por `L.circle` ausente no mock (o círculo do raio em metros reais); adicionado.
- 19:34: 2ª rodada — o cenário da edição sobre-especificava "nenhum fitBounds novo": o REOPEN troca o quadrado por círculos de membro, a `boundsSignature` muda **por design** e um refit do MESMO quadro dispara (no-op visual). Asserção corrigida para o contrato real: a última chamada continua 2 pontos @ `FOCUS_MAX_ZOOM` (a regressão .4.24 cairia aqui como rota inteira @ DEFAULT). **12/12**.
- 19:35–19:37: **Prova de valor por mutação** (local, revertida, nada commitado):
  - Mutação 1 (defeito .4.19/.4.20: teto único `FOCUS_MAX_ZOOM` no ramo externo) → **3 testes caem** (endereço→18, desagrupar→19, membro→18). ✅
  - Mutação 2 (`focusMaxZoom` fora das deps do efeito de fit) → **sobrevive**: hoje todo gesto que muda o zoom também troca o conjunto de modelos (quadrado ↔ círculos), e a `boundsSignature` re-dispara o efeito com o valor novo lido do closure. A dep segue necessária por contrato (comentário no código), mas não é observável por gesto de página — registrado como limite conhecido da rede.
  - Mutação 3 (defeito literal da .4.19 no ramo Original: expandido preso em `FOCUS_MAX_ZOOM`) → **1 teste cai** (original desagrupado→MAX). ✅
- 19:38: Gates: lint acusou 1 formatação prettier no arquivo novo → `--fix`. Suíte completa **660/660**; build OK.

## Decisões Tomadas

- **Estender o arquivo de integração existente**, não criar um segundo: mesma técnica ("Leaflet espionado"), mesma página; a docstring agora declara o arquivo como **o contrato de zoom real** da composição.
- **Asserir a OPÇÃO `maxZoom` passada ao `fitBounds` real**, não um zoom pixel-based: jsdom não computa mapa; a opção é exatamente onde os dois defeitos viveram.
- **Cenário 6 relaxado com a realidade do código:** o refit no REOPEN existe por design (assinatura de modelos); o contrato da .4.24 é "mesmo quadro + mesmo zoom", não "zero chamadas".
- **Cenário 7 (padding) diluído nos cenários 1/2/8** (asserções de `paddingBottomRight` por ramo) em vez de um `it` próprio — mesma cobertura, sem duplicar setup.
- **Sem mock de geolocation** (desvio consciente do plano): o fluxo usado (início por tap armado) não toca GPS.

## Testes (gates rotulados)

- 1º `npx vitest run` (baseline): **652/652**.
- `npx tsc --noEmit`: **APROVADO**
- `npx eslint src/`: **APROVADO** (após `--fix` de 1 formatação prettier)
- `npx vitest run` (suíte completa): **APROVADO** — 64 arquivos, **660/660** (8 novos).
- `npm run build`: **APROVADO**
- **Prova de valor:** mutações 1 e 3 derrubadas pela rede (3 e 1 testes); mutação 2 (dep do efeito) mascarada pela `boundsSignature` — limite documentado.

## O Que NÃO Foi Feito

- **Zoom pixel-based real** (Leaflet de verdade em browser): exigiria Playwright/ambiente com canvas — fora do escopo; o smoke no aparelho continua sendo o gate visual (`npm run deploy:test`).
- **Cobrir a dep `focusMaxZoom` do efeito por teste** — não observável via gesto de página hoje (ver mutação 2); o `RouteMap.test` unitário e o comentário no código seguem sendo a guarda.

## Revisão

Auto-revisão (módulo 21): só `src/__tests__/`; zero código de produto (git diff confirma — as 3 mutações revertidas); sem `any` (handlers tipados `(e?: unknown) => void`); comentários em inglês, cenários nomeados pelo comportamento; nenhum teste existente alterado além do helper adaptado ao registro; knobs pinados de propósito com aviso ⚙️ na docstring.

## Tarefas Geradas pela Revisão

- -

## Aprendizados Para o Projeto

- **O buraco entre "prop provada" e "efeito provado" é onde regressão mora.** O trio agora se completa: `MapPage.test` (decisão da página) + `RouteMap.test` (efeito isolado) + integração (a cadeia inteira até a chamada real).
- **Prova de valor por mutação vale o custo:** 5 minutos de mutação revelaram que 1 dos 3 defeitos simulados não é capturável pela composição (mascarado pela `boundsSignature`) — sem isso, a rede pareceria mais forte do que é.

## Resultado

O zoom por contexto (16/17/18/19) e o padding do painel agora são contrato executável sobre a composição real MapPage → RouteMap, nos dois modos. Os dois defeitos que motivaram a tarefa (.4.19/.4.20), reintroduzidos artificialmente, derrubam a suíte. A RF-006.8 pode mexer na altura do painel com rede armada.
