# TASK-RF-023.5 - Integração + limpeza (fecha o épico RF-023)

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Crítico
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** M/M
- **Data-hora origem:** 05/07/26 21:46
- **Data-hora início:** 07/07/26 12:44
- **Data-hora conclusão:** 07/07/26 12:51
- **Dependências:** TASK-RF-023.3 ✅, 023.4 ✅ (smokes do humano aprovados)
- **REQ/ADR/DT:** RF-12/27/28; design doc §2/§5; TASK-REF-011 (recebe a aposentadoria total do AddressSheet)

## Planejamento Aprovado

Escape por snaps na MapPage (full/half → collapsed; colapsado → sai; seleção intacta) com o handler do RouteMap restrito ao modal legado; `fitBounds` com padding inferior via `bottomObstructionPx` (`PANEL_COLLAPSED_PX` exportado do MapPanel); remoção da variante `inline` morta do AddressSheet (aposentadoria total → REF-011); teste de integração MapPage + RouteMap real; docs RF-12/27/28 + nota §2 do design doc + memória; fechar o épico.

## Execução

- 07/07 12:44: Plano aprovado; ciclo aberto; smoke da .4 registrado como APROVADO.
- 12:45: `MapPanel` exporta `PANEL_COLLAPSED_PX = 132` (mesma fonte do snap). `RouteMap`: prop `bottomObstructionPx` somada ao `paddingBottomRight` dos dois enquadramentos (rota inteira e parada expandida); handler de Escape ganha guard `if (embedded) return` (semântica legada preservada no modal).
- 12:46: `MapPage`: efeito de Escape por snaps (design §5 — nunca limpa a seleção; painel nunca esvazia) + repassa `bottomObstructionPx`.
- 12:47: `AddressSheet`: prop `variant` removida (overlay fixo; doc comment aponta o modal legado como único consumidor e a REF-011 como aposentadoria); testes da variante ajustados.
- 12:48: Testes novos: `MapPage.test` +1 (Escape encadeado: half → collapsed com seleção intacta → navega); **`MapPage.integration.test.tsx`** novo (4) — MapPage + **RouteMap real** com Leaflet espionado (mesmo mock do RouteMap.test): render embedded sem botão de fechar; `fitBounds` com `paddingBottomRight [50, 182]`; clique de marcador real (handler capturado) → painel sobe e item expande com SPX; Escape do RouteMap inativo no embedded (página é dona).
- 12:49–12:51: Docs (requisitos RF-12 ✅ reescrito p/ MapPanel/StopItemList; RF-27/28 → 🟡 com a parte Original ✅ e o restante em RF-006; design doc §2 nota do AddressSheet; REF-011 com escopo ampliado) + gates.

## Decisões Tomadas

- **Teste de integração com Leaflet espionado** (não real): jsdom não roda Leaflet (sem canvas) — mesmo padrão de mock do `RouteMap.test`; a integração exercitada é a fiação MapPage↔RouteMap (estado elevado), que era o objetivo. Limitação anotada no cabeçalho do arquivo.
- **AddressSheet não aposentado aqui**: o modal fullscreen legado (RouteViewer inline) ainda o usa; remover o fluxo é escopo da REF-011 (agora com essa nota explícita). Sem código morto: só a variante `inline` (morta desde a .4) saiu.
- Escape re-registra o listener por mudança de snap (deps) — página única, custo desprezível, sem ref extra.
- Housekeeping: cabeçalhos dos épicos RF-022/RF-023 em pendentes marcados CONCLUÍDOS (o do 022 estava estale como "Pendente").

## Testes (gates rotulados)

- `npx tsc --noEmit`: **APROVADO**
- Suíte completa `npx vitest run`: **APROVADO** — 51 arquivos, **486/486** (antes: 482; +4 líquidos)
- `npm run lint`: **APROVADO**
- `npm run build`: **APROVADO**
- Smoke manual (humano) recomendado na próxima sessão de uso: Escape em desktop (half → collapsed → volta) e rota inteira visível acima do painel ao abrir o mapa.

## Resultado

**Épico TASK-RF-023 concluído**: o mapa Original tem o painel persistente completo da tela 5 — header (modo/steppers/métricas), lista de endereços com drill-down para pacotes, seleção espelhada mapa↔painel, Escape por snaps, enquadramento que respeita o painel e estrutura 100% por slots para os modos do Meu roteiro (RF-006/009/010) sem mudar a casca. Pendências herdadas: REF-011 (fluxo legado + AddressSheet), radius 0.5rem do neonflux (estética).
