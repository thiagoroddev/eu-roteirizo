# TASK-TEST-001 - Testes de integração: fluxo RouteViewer + interações RouteSelector

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** Normal
- **Esforço-H/IA:** M/G
- **Data-hora origem:** 22/06/26 11:05
- **Data-hora início:** 22/06/26 14:47
- **Data-hora conclusão:** 22/06/26 15:01
- **Dependências:** -
- **REQ/ADR/DT:** REV-001-A11
- **Observações:** Achado REV-001-A11. RouteViewer (página orquestradora) e RouteSelector (dropdown) não tinham teste.

## Planejamento Aprovado

Dois arquivos de teste novos (Vitest + Testing Library, `vi.mock`).

### Arquivo 1 — `src/__tests__/components/RouteSelector.test.tsx` (componente real, interação)
- Desabilitado + "Aguardando arquivo" quando vazio; "Escolha uma das N rotas" com rotas; menu fechado inicialmente.
- Abrir menu no clique; listar rotas; selecionar → `onSelect(route)` + fecha.
- Mostrar rota selecionada (+ tipo de veículo); omitir sufixo quando "Sem dados"; fechar ao clicar fora.
- Mock leve de `getVehicleType`.

### Arquivo 2 — `src/__tests__/pages/RouteViewer.test.tsx` (integração)
- Mock dos hooks (`useRouteUploader`/`useRouteSearch`) para controlar dados; stub do `RouteMap` (evita Leaflet).
- Fluxo: render inicial (título + selector) → selecionar rota → RouteSummary aparece → "Ver no Mapa" abre/fecha o mapa → "Tabela Original" abre/fecha o modal.

### Critérios de aceite
- 2 arquivos de teste passando; fluxo principal coberto; `vitest` verde; `tsc -b` exit 0; `lint` exit 0.

## Execução
- 14:47: Movida de pendentes para em-andamento; plano proposto.
- 14:58: Plano aprovado pelo humano ("sim").
- 14:58: Criado `RouteSelector.test.tsx` (7 testes). Ajustado tipo `ComponentProps` (sem depender de React global).
- 14:59: Criado `RouteViewer.test.tsx` (4 testes). Decisão: **RouteSummary real** (não mockado) — funcionou no jsdom, integração mais fiel; só o `RouteMap` foi stubado (Leaflet).
- 15:00: `vitest` 235/235. `tsc -b` exit 0.
- 15:00: `lint` apontou 1 erro de formatação (prettier, array longo) → corrigido (array multilinha). `lint` exit 0.

## Decisões Tomadas
- **Deixar RouteSummary real no teste de integração:** o hook `useRouteSummary` computou sem problemas no jsdom; integração mais valiosa que um stub. Só `RouteMap` foi stubado por causa do Leaflet.
- **Mockar os hooks de dados em vez de simular upload de arquivo real:** controla o estado (`routes`/`availableCols`) de forma determinística; a seleção de rota é exercitada de verdade via UI (RouteSelector real dentro do RouteViewer).

## O Que NÃO Foi Feito (e Por Quê)
- **Simular o upload de arquivo de ponta a ponta** (FileReader/XLSX real): fora do escopo; o parsing já é coberto por `excelProcessor.test.ts`. O foco aqui é a orquestração do RouteViewer e a interação do RouteSelector.

## Testes
- 1º (baseline) `vitest`: 224/224.
- Novos: `RouteSelector.test.tsx` (7) + `RouteViewer.test.tsx` (4) = 11.
- Último `vitest`: **235/235** verdes. `tsc -b`: exit 0. `lint`: exit 0.

## Revisão
Auto-revisão (Standard): cobre a lacuna do A11 (orquestração + dropdown); integração real onde viável (RouteSummary), stub só onde necessário (Leaflet); só adiciona testes, sem tocar produção; gates verdes. OK.

### Achado durante a execução (anti-padrão: não corrigido fora do escopo)
- `src/components/RouteSelector.tsx:66-67` tem strings de UI **hardcoded** (`"-- Aguardando arquivo --"`, `"-- Escolha uma das N rotas --"`) que **duplicam** `UI_LABELS.ROUTE_SELECTOR.WAITING_FILE`/`CHOOSE_ROUTE`. Foi **omitido** pelo inventário da TASK-REF-001 (A05). Não corrigido aqui (fora do escopo de testes) — recomendado como tarefa nova.

## Tarefas Geradas pela Revisão
- **TASK-REF-004 (recomendada, não criada):** usar `UI_LABELS.ROUTE_SELECTOR.WAITING_FILE`/`CHOOSE_ROUTE` em `RouteSelector.tsx` (strings hardcoded que escaparam da TASK-REF-001 / REV-001-A05).

## Requisitos Gerados pela Revisão
- Nenhum.

## ADRs Geradas pela Revisão
- Nenhuma.

## Aprendizados Para o Projeto
- Em teste de integração de orquestrador, mockar só o que é tecnicamente inviável no jsdom (Leaflet) e deixar o resto real dá cobertura mais fiel — RouteSummary real exercitou de fato o caminho upload→seleção→resumo.
- Escrever testes que consultam por `UI_LABELS` (e não por literais) revela componentes que ainda hardcodam texto — aqui expôs o RouteSelector (gap do A05).
