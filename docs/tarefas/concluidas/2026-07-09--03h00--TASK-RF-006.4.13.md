# TASK-RF-006.4.13 - Destaque de seleção: linha selecionada (roteiro) + quadrado da parada (dois modos)

- **Status:** Concluído
- **Modo:** Standard (feedback do humano 09/07)
- **Valor:** Importante
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** M/M
- **Data-hora origem:** 09/07/26 02:55
- **Data-hora início:** 09/07/26 03:00
- **Data-hora conclusão:** 09/07/26 03:10
- **Dependências:** RF-006.4.12 ✅

## Planejamento Aprovado

1. **Painel**: a linha "Endereço selecionado" (âncora no roteiro / órfão) fica **destacada por padrão** (é a selecionada). 2. **Mapa**: destacar o **quadrado da parada que o painel mostra** nos dois modos — via `highlightedStopKey` (só anel/brilho, sem disparar o fitBounds de foco).

## Execução

- 03:00: Ciclo aberto.
- 03:04: `computeMarkerModels` ganhou param `highlightedStopKey?` — o quadrado destacado passa a ser `highlightedStopKey ?? focusedKey` (mantém a ênfase de foco da .4.10 quando não passado). RouteMap: prop `highlightedStopKey` repassada ao `computeMarkerModels` (deps). MapPage: passa `highlightedStopKey={effectivePanelStopKey}` no modo Original (o roteiro já destaca via `selectedStopId` no `computeRoteiroMarkerModels`).
- 03:07: Painel — `RoteiroStopSection` (âncora) e `RoteiroPointSection` (órfão) passam a renderizar a linha "Endereço selecionado" com `highlighted` por padrão (antes só quando `expanded`). O Original (card selecionado + lista com `selectedKey`) segue como estava (é a referência).
- 03:09: Testes — markerModels (`highlightedStopKey` destaca o quadrado da parada do painel sem foco); MapPage (Original passa `highlightedStopKey = effectivePanelStopKey` e acompanha o stepper — `data-highlighted-stop`); RoteiroStopSection (âncora com `aria-current`). Prettier no destructuring do RouteMap.

## Decisões Tomadas

- **`highlightedStopKey` separado do foco** — o destaque do quadrado segue a parada do PAINEL (inclusive no estado inicial, memória), enquanto o `fitBounds` de foco continua só no clique/stepper (senão daria zoom na carga). No mapa o `markerSvg` já desenha o anel (`selected`) + brilho (`emphasis`) em qualquer forma, inclusive quadrado.
- **Só o roteiro mudou no painel** — o Original já destaca o selecionado (referência do humano); alinhei o roteiro (âncora/órfão destacados por padrão).
- **Roteiro já destacava o quadrado** (`selectedStopId` desde a .4.2) — o gap era o Original (sem destaque do quadrado da parada do painel), agora coberto pelo `highlightedStopKey`.

## Testes (gates rotulados)

- `npx tsc --noEmit`: **APROVADO**
- Suíte completa `npx vitest run`: **APROVADO** — 63 arquivos, **622/622** (620 + 2 líquidos; run limpo). Nota: 1 timeout intermitente do flake conhecido `useRouteUploader` >10MB numa rodada sob carga — 18/18 verde isolado e no re-run completo.
- `npm run lint`: **APROVADO**
- `npm run build`: **APROVADO**
- ⚠️ **Smoke manual (humano):** (1) no Meu roteiro, selecionar uma parada → a linha "Endereço selecionado — parada do veículo (âncora)" fica **destacada** (fundo accent) indicando que é a selecionada; idem no órfão; (2) no mapa (Original E roteiro), o **quadrado da parada que o painel mostra** fica com **anel + brilho** — inclusive na parada inicial (sem clique) e acompanhando o stepper ‹ ›; (3) conferir contraste do destaque no tema dark; (4) opcional: avaliar destacar também o membro selecionado na lista completa do roteiro (não feito — sem estado de "membro selecionado" ainda).

## Resultado

A seleção passou a ser visível: a linha "Endereço selecionado" do Meu roteiro fica destacada, e o quadrado da parada que o painel mostra ganha anel + brilho nos dois modos (no Original inclusive sem clique, seguindo o painel). Fecha o feedback de destaque.
