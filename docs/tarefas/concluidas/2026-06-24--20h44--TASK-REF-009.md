# TASK-REF-009 - Identidade visual final "Cyanide Precision" (ciano + claro/escuro automático + pill + toggle)

- **Status:** Concluído
- **Modo:** Strict
- **Valor:** Importante
- **Urgência:** Normal
- **Esforço-H/IA:** P/G
- **Data-hora origem:** 24/06/26 20:10
- **Data-hora início:** 24/06/26 20:10
- **Data-hora conclusão:** 24/06/26 20:44
- **Dependências:** TASK-REF-008 (azul interino), ADR-004
- **REQ/ADR/DT:** ADR-006 (revisada); `tema-tailwind.md`; RNF-08/09

## ADR Vinculada
- **ADR-006** (Aceita, revisada): identidade **"Cyanide Precision"** — ciano `#0DC2D6`, claro+escuro automático, pill. Supersede a cor da ADR-004 e o azul interino da REF-008.

## Planejamento Aprovado

O humano escolheu (sobre o tema gerado "Cyanide Precision"): **dark mais bonito**, **primário ciano `#0DC2D6`**, e respondeu às 2 perguntas: **claro + escuro (auto por sistema)** e **formato arredondado/pill**.

Implementar:
1. **Tokens** (`theme.css`): claro (`:root`, ciano sobre fundo claro) + escuro (`.dark`, Cyanide slate/navy). Primário ciano com **texto escuro** no botão.
2. **Pill:** `--radius` 1rem + `button.tsx` `rounded-full`.
3. **Troca de tema:** automático (sistema) + toggle manual, persistido, sem flash.
4. ADR-006 (revisar) + `tema-tailwind.md` (spec) + `prompt` §2.

## Execução

- 24/06 20:10: `theme.css` reescrito (claro + escuro Cyanide; `--primary 186 88% 44%` / `--primary-foreground 187 100% 12%`; `--radius 1rem`).
- `button.tsx` → `rounded-full` (pill). `index.html` → script inline anti-flash + fonte.
- Criados `services/themeService.ts` (storage/DOM **defensivo**), `hooks/useTheme.ts`, `components/ThemeToggle.tsx` (toggle provisório no `RouteViewer`, top-right).
- `setupTests.ts` → mock de `matchMedia` (jsdom).
- Gate: 4 testes do `RouteViewer` quebraram (`localStorage.getItem is not a function` no jsdom) → `themeService` envolvido em try/catch → verde.
- ADR-006 revisada (azul→ciano final); `tema-tailwind.md` e `prompt` §2 alinhados.
- 20:44: gates finais.

## Decisões Tomadas
- **Ciano com texto escuro** (`--primary-foreground` `#00363D`): ciano + branco reprovaria contraste; ciano + escuro passa (padrão Cyanide).
- **Claro + escuro automático** (não só escuro): o uso é de **rua/dia**, e o escuro lava ao sol — o claro garante legibilidade de dia; auto pelo sistema.
- **`themeService` defensivo:** `localStorage`/`matchMedia` em try/catch — corrige o jsdom e protege modo privado/indisponível.
- **Toggle provisório no `RouteViewer`** (fixed top-right): a casa definitiva é o app shell (RF-011), com rótulos via `UI_LABELS`.

## O Que NÃO Foi Feito (e Por Quê)
- **Self-host da fonte:** adiado (offline cai no fallback; não bloqueia).
- **Tiles escuros no mapa:** mantidos claros (padrão: mapa claro + chrome escuro).
- **Migrar componentes:** desnecessário — tudo consome tokens.

## Testes
- `npm run build`: **APROVADO** — tsc + vite build (8.45s); PWA gerado; ícones lucide (Sun/Moon/Monitor) ok.
- `npm run lint`: **APROVADO** — eslint sem erros.
- `npm run test` (vitest): **APROVADO** — **249/249** (após o guard defensivo + mock de matchMedia).

## Revisão
Auto-revisão (Strict): tokens dos dois temas coerentes; primário ciano com texto escuro (contraste); troca automática + manual, persistida, sem flash; storage isolado em `services/` e defensivo; pill aplicado; build/lint/test verdes. OK.

## Tarefas Geradas pela Revisão
- (opcional) Self-host da fonte Hanken Grotesk (offline pleno).
- Toggle de tema definitivo no app shell (RF-011) com `UI_LABELS`.

## Requisitos Gerados
- Nenhum novo (RNF-08/09 de design já existiam; agora apontam para a identidade Cyanide).

## ADRs Geradas
- ADR-006 (revisada nesta tarefa para a decisão final).

## Aprendizados Para o Projeto
- **Tema é token, não componente:** trocar marca, claro/escuro e até o raio foi editar `theme.css` + 1 classe no botão. Dividendo do shadcn (ADR-004).
- **jsdom é traiçoeiro com browser APIs:** `matchMedia` e `localStorage` faltam/quebram nos testes — serviço **defensivo** + mocks no setup resolvem (e ainda blindam o runtime).
- **Decisão de tema ≠ só estética:** "escuro é mais bonito" colide com "uso ao sol"; manter os dois + auto reconcilia gosto e função.
