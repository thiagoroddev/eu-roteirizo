# TASK-DOC-003 - Sincronizar `contexto-projeto-ai.md` com o código atual

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** Normal (antecipada a pedido do humano, 10/07: "TASK-DOC-003 agora")
- **Esforço-H/IA:** P/P
- **Data-hora origem:** 22/06/26 23:50
- **Data-hora início:** 10/07/26 17:40
- **Data-hora conclusão:** 10/07/26 18:05
- **Dependências:** TASK-RF-011 ✅
- **REQ/ADR/DT:** ADR-003; núcleo §2.3 — o arquivo é o **topo da hierarquia de regras**, e doc que contradiz o código é tóxica

## Planejamento Aprovado

O arquivo estava congelado em 24/06 e mentia em pontos centrais: "SPA de página única sem router" (falso desde a RF-011), ~240 testes (639), "sem `divida-tecnica.md`" (existe), "`docs/design/` não existe" (existe), pacote com extensão "`.md.md`" (renomeado na v4.0) — e ignorava o router/shells, `services/`, `utils/routing|markers`, o épico RF-006 inteiro, as ADRs 002–009, o tema Neon Flux, vaul/idb/lucide e o deploy de testes.

**Método:** fatos levantados do código (package.json, `ls src/`, rotas do `App.tsx`, títulos das 9 ADRs) → reescrita seção a seção mantendo a estrutura. Sem decisão nova — só sincronização.

## Execução

- 17:40: Fatos levantados do código.
- 17:50: Reescrita completa. Por seção: **descrição** (visualizador + roteirizador em construção, multi-tela, IndexedDB); **stack** (+router 7, vaul, idb, lucide, fake-indexeddb, wrangler-via-npx; notas de versão reais); **estrutura** (4 pages, `services/`, `components/{ui,shell,map/panel,manifests,summary}`, `utils/routing|markers`, `lib/`, 639 testes); **Rotas** (tabela das 4 rotas + shells + deep links + `_redirects`); **Decisões Inegociáveis** (+ADR-002 roteamento local sem API paga, ADR-009 interop, ADR-008 paleta travada + carro azul do início, ADR-006 rev. Neon Flux dark, `useReducer` por feature, IndexedDB); **Convenções** (utils=puro × services=IO — a nota antiga "utils no lugar de services" caiu; padrão ⚙️ MANUAL KNOB; smoke no aparelho como gate de UI + URL de testes); **NÃO Faz** (RF-008/009/012/013 pendentes; rascunhos: HubFlow = visão, MAS fluxo-roteirizacao/modo-original = **specs vigentes**); **Documentação** (specs + design doc + dívida técnica + fila no pendentes.md); **Hierarquia** (nota `.md.md` removida); **Estado Atual** (fase real do épico, fila, 639/639, gates padrão).
- 18:00: Rodapé "Última Atualização" com o escopo desta sync e gatilhos da próxima (RF-008 ou RF-009, ou ~2 meses).

## Decisões Tomadas

- **"Rascunhos" deixou de ser um rótulo único:** o aviso antigo tratava `docs/rascunhos/` inteiro como "visão futura, não implementado". Hoje é meia-verdade — o HubFlow segue visão, mas `fluxo-roteirizacao.md`/`fluxo-modo-original.md` são as **specs vigentes** citadas por dezenas de tarefas. O arquivo agora distingue os dois, porque é exatamente o tipo de ambiguidade que confunde um agente novo.
- **Contagens datadas, não absolutas:** "639/639 (10/07/26)" e "npm audit 1 low (última checagem 24/06)" — números sem data foram o que apodreceu a versão anterior.
- **Convenções ganharam o que o código já pratica** e nenhum doc registrava: o padrão ⚙️ MANUAL KNOB e o smoke no aparelho como gate de UI (lição das RF-006.4.19/.4.20).

## Testes (gates rotulados)

- Gates de código: **N/A** — somente `docs/contexto-projeto-ai.md`; nenhum `.ts`/`.tsx` tocado (suíte permanece 639/639 do gate anterior, mesma árvore).
- Verificação: cada afirmação nova é rastreável a uma leitura feita nesta sessão (deps do `package.json`, `ls src/`, `App.tsx:23-32`, títulos das ADRs 001–009, existência de `docs/design/` e `docs/dominios/`).

## O Que NÃO Foi Feito

- **`README.md`/`CODIGO_COMENTADO.md`/`BOAS_PRATICAS.md`** não foram auditados — podem ter defasagens próprias; se incomodar, é outra TASK-DOC.
- Nenhuma decisão nova foi tomada — divergências que exigiriam decisão (ex.: formalizar "Carregando ruas…") continuam nas suas tarefas (`DOC-005`).

## Aprendizados Para o Projeto

- **O arquivo-topo da hierarquia precisa de gatilho de revisão por EVENTO, não só por tempo.** Ficou 16 dias mentindo sobre a arquitetura porque a revisão era "em ~3 meses". O rodapé novo amarra a próxima sync às tarefas que mudam a natureza do app (RF-008/RF-009).

## Resultado

O `contexto-projeto-ai.md` volta a ser confiável como fonte nº 1: descreve o app multi-tela com os dois modos do mapa, a stack real, as 9 ADRs, o estado verdadeiro do épico RF-006 e as convenções que o código já pratica.
