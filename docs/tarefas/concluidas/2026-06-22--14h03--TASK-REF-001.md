# TASK-REF-001 - Alinhar código à ADR-001 (comentários PT→EN; nenhuma string de UI hardcoded)

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Desejável
- **Urgência:** Normal
- **Esforço-H/IA:** M/M
- **Data-hora origem:** 22/06/26 11:05
- **Data-hora início:** 22/06/26 13:57
- **Data-hora conclusão:** 22/06/26 14:03
- **Dependências:** -
- **REQ/ADR/DT:** REV-001-A05, ADR-001
- **Observações:** Achado REV-001-A05 / ADR-001. Refactor puro — strings mantêm o mesmo valor, sem mudança de comportamento.

## Planejamento Aprovado

Inventário verificado (8 strings de UI hardcoded em 5 arquivos + 6 comentários em PT em 2 arquivos).

### Parte 1 — Strings de UI → UI_LABELS
1. `RouteSearchByAT.tsx` (`:15`, `:23`): trocar literais `"🔍 Busca por código AT..."` e `"🚫 Rota não encontrada."` por `UI_LABELS.ROUTE_SEARCH.PLACEHOLDER` e `.NOT_FOUND` (já existem). Importar `UI_LABELS`.
2. `ExampleTable.tsx` (`:16`): usar `UI_LABELS.EXAMPLE_TABLE.TITLE` (já existe). Importar `UI_LABELS`.
3. `RouteViewer.tsx` (`:178`): adicionar `UI_LABELS.ROUTE_VIEWER.TITLE = "Pré-Rota"` e referenciar.
4. `RouteMap.tsx`: adicionar seção `UI_LABELS.ROUTE_MAP` com os valores atuais e referenciá-los:
   - `CLOSE`: "Fechar Mapa"; `FULLSCREEN_ARIA`: "Mapa em tela cheia".
   - `TOOLTIP`: { SEQUENCE, STOP, ADDRESS, NEIGHBORHOOD, ZIPCODE, COMMERCIAL, CORREIOS }.
   - Usar no template do tooltip, no `aria-label` e no botão.

### Parte 2 — Comentários PT → EN
- `RouteMap.tsx`: 4 comentários. `formatters.ts`: 2 comentários.
- (Conversão pontual — são poucos; coerente com a nota da ADR-001 de que não há tradução em massa.)

### Critérios de aceite
- Nenhuma string de UI hardcoded nos 5 arquivos; todas vêm de `UI_LABELS` (valores idênticos → sem mudança visível).
- Os 6 comentários em PT convertidos para EN.
- `vitest` 224/224; `tsc -b` exit 0; `lint` exit 0.

## Execução
- 13:57: Movida de pendentes para em-andamento; plano proposto.
- 14:00: Plano aprovado pelo humano ("sim").
- 14:01: `uiLabels.ts` — adicionadas seções `ROUTE_VIEWER` e `ROUTE_MAP` (com TOOLTIP).
- 14:01: `RouteSearchByAT.tsx` — import + `PLACEHOLDER`/`NOT_FOUND`.
- 14:01: `ExampleTable.tsx` — import + `EXAMPLE_TABLE.TITLE`.
- 14:02: `RouteViewer.tsx` — import + `ROUTE_VIEWER.TITLE`.
- 14:02: `RouteMap.tsx` — tooltip/botão/aria via `UI_LABELS.ROUTE_MAP`; 4 comentários PT→EN.
- 14:02: `formatters.ts` — 2 comentários PT→EN.
- 14:03: Gates → `vitest` 224/224; `tsc -b` exit 0; `lint` exit 0.

## Decisões Tomadas
- **Centralizar os rótulos do tooltip do mapa em `UI_LABELS.ROUTE_MAP.TOOLTIP`:** eram texto de UI embutido em string HTML; agora seguem a mesma regra das demais labels (i18n-ready).
- **Rótulos estáticos do tooltip não passam por `escapeHtml`:** são constantes controladas (não dados do usuário); apenas os valores interpolados de células continuam escapados (TASK-BG-003).
- **Manter os mesmos valores exatos ao mover para `UI_LABELS`:** refactor sem mudança de comportamento; preserva testes que consultam por texto.

## O Que NÃO Foi Feito (e Por Quê)
- **Hint de lint "Button type attribute has not been set"** em `RouteSearchByAT.tsx` (botão do resultado): pré-existente e fora do escopo desta tarefa (severidade Hint; `lint` continua exit 0). Pode virar TASK-REF/TASK-BG menor se desejado.
- **Varredura exaustiva de TODOS os comentários do projeto:** a ADR-001 define conversão PT→EN oportunística (sem tradução em massa). Tratados apenas os 6 comentários PT que o inventário encontrou.

## Testes
- 1º (baseline) `vitest`: 224/224.
- Último `vitest`: 224/224 (sem testes novos — refactor; valores de label inalterados mantêm verdes os testes que consultam por texto, ex.: `RouteSearchByAT.test.tsx`).
- `tsc -b`: exit 0. `lint`: exit 0.

## Revisão
Auto-revisão (Standard): refactor localizado; remove duplicação de literais e centraliza UI em `UI_LABELS` (cumpre ADR-001); valores preservados; gates verdes. OK.

## Tarefas Geradas pela Revisão
- Nenhuma criada. (Possível TASK menor: corrigir o hint de `type` do botão em RouteSearchByAT.)

## Requisitos Gerados pela Revisão
- Nenhum.

## ADRs Geradas pela Revisão
- Nenhuma. (Implementa a ADR-001.)

## Aprendizados Para o Projeto
- Texto de UI dentro de strings HTML (tooltips/popups) é fácil de esquecer numa auditoria de "strings hardcoded"; vale incluir templates HTML no checklist de i18n, além do JSX.
- Centralizar labels em `UI_LABELS` antes de um eventual segundo idioma deixa a internacionalização a um arquivo de distância (ADR-001).
