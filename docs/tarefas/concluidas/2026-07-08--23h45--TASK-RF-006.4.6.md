# TASK-RF-006.4.6 - Meu roteiro: "Criar parada" comita na hora + membros do raio + stepper no preview (fatia 1 do epic .4.6)

- **Status:** Concluído
- **Modo:** Standard (epic .4.6, fatia 1 — plano aprovado em plan mode)
- **Valor:** Crítico
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** M/G
- **Data-hora origem:** 08/07/26 22:20
- **Data-hora início:** 08/07/26 22:25
- **Data-hora conclusão:** 08/07/26 23:45
- **Dependências:** RF-006.4.5 ✅
- **REQ/ADR/DT:** fluxo §4/§8 (reverte "candidatos entram por escolha", 26/06); plano `como-resolver-o-shell-graceful-gadget.md`; epic reformula RF-023/absorve parte da RF-006.6 (fatias .4.7/.4.8 seguintes)

## Planejamento Aprovado

Fatia 1 do epic (plano aprovado): "Criar parada" passa a **comitar na hora** já com os endereços do raio como membros (reverte §8), com o raio ajustável **antes** de criar (stepper no preview "Parada sugerida"). Foco/expandir por 1 vs 2 cliques e edição sem toggle ficam nas fatias .4.7/.4.8.

- **Reducer** `builder.ts`: ação `CREATE_STOP { seedPointId, memberIds, vehicleStop, radiusMeters }`.
- **Preview** `RoteiroPointSection.tsx`: stepper de raio na "Parada sugerida".
- **MapPage**: `previewRadiusMeters` ephemeral; `handleCreateStop` → `CREATE_STOP` + foca a parada firmada.

## Execução

- 22:25: Ciclo aberto.
- 22:40: `CREATE_STOP` no reducer — monta `RouteStop` comitado direto (`sweepWalkingOrder(vehicleStop, [seed, ...radiusMembers])`, `normalizeOrders`), sem draft; de-dupa a semente, filtra pontos já assinalados, no-op p/ semente desconhecida/tomada ou stopId já existente; limpa `nextSuggestionOverride`.
- 23:00: `RadiusStepper` extraído de `RoteiroDraftSection` (DRY) e usado no card do raio da edição E na seção "Parada sugerida" do preview. `neon` propagado em `StopItem`/`StopItemList` (já vinha da .4.3).
- 23:15: MapPage — `previewRadiusMeters` (default `config.autoRadiusMeters`), reset por órfão selecionado; `previewCandidateIds` + `roteiroOverlay.radiusCircle` passam a usá-lo; `handleCreateStop` dispata `CREATE_STOP` com `seed + previewCandidateIds` + `suggestVehicleStop(graph, seed)` + `previewRadiusMeters`, e foca a parada firmada (`selectedStopId = stop_<seed>`), sem abrir draft. Efeito da decisão C anotado como inerte (create-time snapping cobre o caso; volta na .5).
- 23:30: Testes — builder `CREATE_STOP` (2 casos: firma+varre; de-dupa/ignora assinalados/no-ops); RoteiroPointSection (props do stepper + 1 teste do stepper no preview); MapPage: 8 testes do fluxo criar→draft **revisados por definição** (create agora comita — sem draft; auto-inclui candidatos do raio; stepper no preview redimensiona círculo/contagem; criar P1/P2 firmam direto; editar via REOPEN p/ save-vazio/cancel; incorporar; tocar parada→Editar/Desfazer; círculo cresce/clampa). O teste da decisão C (draft anchor) foi substituído pelo teste do círculo do preview (o create-draft que o disparava deixou de existir).

## Decisões Tomadas

- **`CREATE_STOP` dedicado** (em vez de OPEN+COMMIT encadeados) — atômico e testável; espelha o id `stop_<seed>` do OPEN.
- **Reversão do §8 documentada** — o que o raio mostra entra como membro ao criar; o refino fino (tirar/adicionar, raio) fica no REOPEN (Editar parada) e nas fatias seguintes.
- **Raio no preview** (não só na edição) — o usuário calibra ANTES de firmar; `previewRadiusMeters` é efêmero (reseta ao default por órfão).
- **Decisão C (re-projeção da âncora quando o grafo chega) fica inerte** nesta fatia — a âncora é resolvida no create (`suggestVehicleStop`, snapa se o grafo já chegou); âncora criada offline é provisória até a .5 (gestos da âncora). Efeito mantido e anotado.

## Testes (gates rotulados)

- `npx tsc --noEmit`: **APROVADO**
- Suíte completa `npx vitest run`: **APROVADO** — 63 arquivos, **606/606** (603 + 3 líquidos; ~10 revisados por definição no MapPage).
- `npm run lint`: **APROVADO**
- `npm run build`: **APROVADO**
- ⚠️ **Smoke manual (humano), fatia 1:** (1) tocar um endereço livre com vizinhos no raio → "Parada sugerida" com o **stepper de raio**; ajustar o raio muda o círculo e a contagem ao vivo; (2) "Criar parada" → parada **firmada e agrupada** (um quadrado) + painel de **resumo** focado, **sem** cair na "Edição de parada"; os endereços do raio entraram sozinhos; (3) só o que ficou fora do raio segue livre (HUD "Faltando"); (4) "Editar parada" ainda reabre o rascunho (toggle no mapa continua aqui — sai na .4.8); (5) criar P1 e P2 em sequência firma as duas.

## O Que NÃO Foi Feito (fatias seguintes do epic)

- **.4.7** — modelo compartilhado foco/expandir por 1 vs 2 cliques (Original + base do roteiro); muda o contrato do RF-023.
- **.4.8** — Meu roteiro: expandir a parada firmada (2 cliques) + editar sem toggle no mapa (remove `TOGGLE_DRAFT_POINT` do tap; edição desagrupada com raio + pontilhados).

## Resultado

"Criar parada" agora firma a parada na hora, já com os endereços que o raio mostrava (reverte §8), e o raio é calibrado antes de criar no próprio preview. O mapa mostra o quadrado agrupado (comportamento de parada firmada já existente) e o painel foca o resumo. As duas metades restantes do pedido do humano (expandir por 2 cliques e editar sem toggle no mapa) vêm nas fatias .4.7/.4.8.
