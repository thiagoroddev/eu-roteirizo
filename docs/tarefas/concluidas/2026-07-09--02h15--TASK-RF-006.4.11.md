# TASK-RF-006.4.11 - Desacoplar duplo-clique (desagrupa mapa) da lista completa (esconde mapa) + foco perto

- **Status:** Concluído
- **Modo:** Standard (feedback do humano 09/07 sobre a .4.10)
- **Valor:** Importante
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** M/G
- **Data-hora origem:** 09/07/26 02:10
- **Data-hora início:** 09/07/26 02:15
- **Data-hora conclusão:** 09/07/26 02:25
- **Dependências:** RF-006.4.10 ✅
- **REQ/ADR/DT:** ajuste do modelo de interação da .4.10 (RF-023)

## Planejamento Aprovado

1. **Duplo-clique = SÓ desagrupa a parada no mapa** (não abre a lista completa do painel — a lista esconde o mapa; eventos distintos). Regrupar = clicar fora do grupo no mapa. 2. **Foco perto**: o zoom do 1º clique estava longe demais — usar o MESMO de quando desagrupa. 3. Clicar fora regrupa mantendo o foco.

## Execução

- 02:15: Ciclo aberto.
- 02:18: RouteMap — foco (Original) agora usa `maxZoom: MAX` + padding [40,40] (= expand; antes DEFAULT/[50,50], "muito distante"); prop `focusBounds?: LatLng[]` (roteiro) — quando presente, o fit externo dá zoom CLOSE (MAX) na parada em vez de enquadrar tudo; `focusBoundsRef` + `focusSignature` no boundsSignature (refit ao mudar o foco).
- 02:21: MapPage — `handleExpandRoteiroStop` (duplo-clique) passa a só setar `expandedRoteiroStopId` + focar (SEM panelView "list"/snap); "Ver lista completa" vira `handleShowRoteiroList`/`handleHideRoteiroList` (evento distinto: abre/fecha a lista, regrupa ao fechar); `handleShowList` (Original) deixa de expandir o mapa (só abre a lista); `handleMapTap` — se há parada desagrupada, clicar fora REAGRUPA mantendo o foco (senão deseleciona); `focusBounds` passado ao RouteMap = coords dos membros da parada selecionada.
- 02:24: Testes — RouteMap (`focusBounds` → fit MAX; foco interno já coberto); MapPage revisados por definição: duplo-clique só desagrupa o mapa (painel fica no resumo) + clicar-fora regrupa mantendo foco + `data-focus-bounds`; "Ver lista completa" (roteiro e Original) abre a lista SEM expandir o mapa (evento distinto). Stub ganhou `focusBounds`/`data-focus-bounds`.

## Decisões Tomadas

- **Duplo-clique e "Ver lista completa" são eventos distintos** — o 1º desagrupa no mapa (painel fica no resumo), o 2º abre a lista no painel (esconde o mapa). Nunca acoplados (a .4.10 os acoplava).
- **Foco = mesmo zoom do expand** (MAX, perto) — o DEFAULT ficava "muito distante"; agora focar e desagrupar enquadram igual (só muda agrupado × individual).
- **Roteiro ganhou `focusBounds`** (zoom na parada) — o roteiro enquadrava sempre a rota inteira; ao focar/desagrupar uma parada firmada agora aproxima nela (como no Original).
- **Clicar fora regrupa mantendo foco** — só desagrupa o mapa some; a parada segue selecionada no painel (só um 2º clique fora, sem nada desagrupado, deseleciona).

## Testes (gates rotulados)

- `npx tsc --noEmit`: **APROVADO**
- Suíte completa `npx vitest run`: **APROVADO** — 63 arquivos, **620/620** (619 + 1 líquido; 3 do MapPage revisados por definição — desacoplamento).
- `npm run lint`: **APROVADO**
- `npm run build`: **APROVADO**
- ⚠️ **Smoke manual (humano):** (1) **2 cliques** numa parada firmada → **desagrupa SÓ no mapa** (círculos), o **painel FICA no resumo** (não abre a lista); (2) **clicar fora** do grupo no mapa → **reagrupa** (a parada segue focada no painel); (3) **"Ver lista completa"** (botão) → abre a lista no painel (esconde o mapa) — evento SEPARADO do duplo-clique; "Esconder lista" volta ao resumo; (4) **foco perto**: 1 clique numa parada aproxima nela com o MESMO zoom de quando desagrupa (não mais "distante"); vale Original (fit MAX) e roteiro (focusBounds); (5) conferir no device o zoom (MAX pode ficar perto demais em parada de 1 endereço — calibrar se preciso).

## Resultado

Duplo-clique (desagrupa o mapa) e "Ver lista completa" (abre a lista, esconde o mapa) agora são eventos distintos, como o humano pediu; clicar fora regrupa mantendo o foco. O zoom do foco ficou perto (igual ao de desagrupar) nos dois modos — o roteiro ganhou `focusBounds` para aproximar da parada. Fecha o feedback da .4.10.
