# TASK-RF-006.4.25 - Edição de parada: sem "Faltando" global + card do raio fixo na primeira seção

- **Status:** Concluído
- **Modo:** Standard (feedback do humano 10/07 — rodada da .4.23/.4.24, mesma área)
- **Valor:** Importante
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** P/P
- **Data-hora origem:** 10/07/26 12:45
- **Data-hora início:** 10/07/26 12:47
- **Data-hora conclusão:** 10/07/26 13:05
- **Dependências:** RF-006.4.24 ✅ (foi o pick no cabeçalho que expôs o card "andando"), RF-006.4.2 ✅ (que havia decidido manter o "Faltando" na edição — **supersede**)
- **REQ/ADR/DT:** RF-23; feedback do humano sobre a .4.23/.4.24 (prints 10/07)

## Planejamento Aprovado

Dois pedidos do humano (rodada de feedback com prints):

1. _"A informação 'Faltando 45 endereços...' não precisa aparecer em edição de parada."_ — supersede a decisão da RF-006.4.2 ("o HUD global fica visível durante a edição"). A edição é sobre UMA parada; o HUD do roteiro inteiro volta com o resumo ao fechar.
2. _"O card de edição de raio muda de lugar ao selecionar um endereço; ele deve ser fixo na primeira seção."_ — o card vivia no **corpo** do painel, e o pick da .4.24 (que entra no **cabeçalho**) o empurrava para baixo quando aparecia.

## Execução

- 12:47: `RoteiroDraftHeader` — linha `ROTEIRO_REMAINING` removida (e as props `remainingAddresses`/`remainingPackages`); o **card do raio inteiro** (stepper + linha de candidatos + aviso RN-17) mudou do `RoteiroDraftBody` para dentro do cabeçalho, após o hint — posição fixa, antes do pick. Props do raio migraram do corpo para o cabeçalho.
- 12:52: `MapPage` — os dois usos atualizados (cabeçalho ganha `radiusMeters`/`onRadiusChange`/`candidateCount`/`farWarning`; corpo fica só com as duas listas).
- 12:56: `RoteiroDraftSection.test` — testes do raio/aviso migraram do corpo para o cabeçalho; asserção nova de **ausência** do "Faltando"; corpo testa só as listas + empty hint.
- 13:00: `MapPage.test` — as 3 asserções que usavam o "Faltando" como proxy de "membership intocada" no contexto de edição trocadas por proxy direto: presença/ausência do botão `−` (`REMOVE_POINT`) do endereço na lista de escolhidos.

## Decisões Tomadas

- **Card dentro do `RoteiroDraftHeader`, não composição no MapPage.** "Fixo na primeira seção" vira estrutural: o card faz parte do cabeçalho do draft e nenhum conteúdo condicional (pick) pode empurrá-lo. Efeito colateral desejado: o colapsado fit-content passa a mostrar o controle de raio sempre — é a ferramenta principal da edição.
- **Proxy de teste melhorado de tabela.** O "Faltando" era usado nos testes como termômetro de "nada mudou"; com ele fora da edição, o proxy virou o `−` da lista — que afirma a membership **diretamente**, e não por efeito indireto.
- **Supersede registrada:** a RF-006.4.2 tinha decidido manter o HUD global na edição; este registro anota a reversão a pedido do humano (10/07), para o histórico não parecer contradição acidental.

## Testes (gates rotulados)

- `npx tsc --noEmit`: **APROVADO**
- `npx eslint src/`: **APROVADO**
- Suíte completa `npx vitest run`: **APROVADO** — 63 arquivos, **635/635**.
- `npm run build`: **APROVADO**
- ⚠️ **Smoke manual (humano):** entrar em "Editar parada" → sem a linha "Faltando: X · Y"; o card "Raio de agrupamento" aparece logo sob o título/chips e **não se move** ao tocar um endereço (o "Endereço selecionado" entra **abaixo** dele); o painel colapsado mostra título + chips + raio; `±` do raio funciona como antes.

## O Que NÃO Foi Feito

- O "Faltando" segue nos demais contextos do roteiro (`RoteiroPanelHeader`) — o pedido era só a edição.
- Nenhuma mudança no reducer nem no fluxo de criação.

## Aprendizados Para o Projeto

- **Conteúdo condicional acima de controle fixo = layout que "anda".** Quando uma seção aparece/some por interação (o pick), tudo que estiver depois dela no mesmo fluxo desliza. Controles permanentes devem vir **antes** de qualquer bloco condicional do slot.

## Resultado

A edição de parada mostra só o que é dela: título, chips, o card do raio fixo na primeira seção (visível até no painel colapsado) e, abaixo dele, o endereço tocado com o botão de adicionar — sem o "Faltando" global e sem nada mudando de lugar.
