# TASK-RF-006.4.4 - Preview agregado da Parada sugerida + remoção da linha de sugestão

- **Status:** Concluído
- **Modo:** Standard (pós-smoke da .4.3)
- **Valor:** Alto
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** P/M
- **Data-hora origem:** 08/07/26 21:30
- **Data-hora início:** 08/07/26 21:35
- **Data-hora conclusão:** 08/07/26 21:45
- **Dependências:** RF-006.4.3 ✅
- **REQ/ADR/DT:** fluxo §8 (nota ".4.4" anexada ao changelog da 3ª rodada)

## Planejamento Aprovado

(1) O resumo da "Parada sugerida" agrega **semente + candidatos do raio** ("se no mapa tem 4, no resumo tem 4") — ordem = `sweepWalkingOrder` ao redor da âncora sugerida (mesma regra do reducer); chips "N endereços" + por tipo + estimativa do circuito com todos; a linha "O raio engloba N candidatos" permanece (explica a conta). Nuance registrada: **"Criar parada" continua semeando só o selecionado** (§8, decisão 26/06 — candidatos entram por escolha). (2) Linha de texto "Sugestão: …" removida do contexto endereço-selecionado (redundante com a seção; permanece no fluxo do início — RF-30 — e a tracejada do mapa continua). (3) "Carregando ruas…" mantido — explicado ao humano: é o status do download do grafo OSM (TASK-RF-006.3; RF-30/RN-18/RNF-13/14 + ADR-002/009); a linha em si foi decisão de implementação da .3, não requisito formal.

## Execução

- 21:35: MapPage: `suggestedPoints` (semente + candidatos do preview, varridos ao redor de `suggestedAnchor`); `suggestedEstimate`/`suggestedPlace`/`suggestedMetrics` sobre o agregado; linha `suggestionLabel` removida do bloco point-selected; import `sweepWalkingOrder`.
- 21:40: Testes: tela 8 e2e revisado (chips "2 endereços" agregados; `queryByText(/^Sugestão:/)` ausente no contexto do ponto); teste do re-apontar revisado por definição — o stub do RouteMap ganhou `data-suggestion-target` (último ponto do path) e a prova do re-apontamento passou da linha de texto para o overlay; seção "Parada sugerida" visível após o tap.

## Decisões Tomadas

- **Preview mostra o potencial do raio; a criação segue §8** — o resumo agregado informa "como ficaria" se os candidatos entrarem, mas o rascunho abre com 1 escolhido + N candidatos (decisão 26/06 do humano intocada).
- **Prova de re-apontamento via overlay** (`data-suggestion-target` no stub) — a linha de texto deixou de existir no contexto; o comportamento (§6) continua coberto.

## Testes (gates rotulados)

- `npx tsc --noEmit`: **APROVADO**
- Suíte completa `npx vitest run`: **APROVADO** — 63 arquivos, **603/603** (2 revisados por definição).
- `npm run lint`: **APROVADO**
- `npm run build`: **APROVADO**
- ⚠️ **Smoke manual (humano):** (1) tocar endereço com vizinhos no raio → resumo da "Parada sugerida" com a MESMA contagem do círculo (ex.: 4 endereços), chips por tipo e estimativa do circuito agregado; (2) sem a linha "Sugestão: …" no painel do endereço (a tracejada continua no mapa; no fluxo do início a linha permanece); (3) criar a parada → edição abre com 1 escolhido + candidatos (comportamento §8 preservado).

## Resultado

O preview da "Parada sugerida" agora conta o que o círculo mostra (semente + candidatos, com estimativa do circuito completo), mantendo a regra de que candidatos só entram por escolha; a linha de sugestão redundante saiu do contexto do endereço. Próxima: **RF-008 antecipada**.
