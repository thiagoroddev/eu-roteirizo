# TASK-DOC-001 - Criar docs/contexto-projeto-ai.md (ponto de entrada do projeto)

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** Normal
- **Esforço-H/IA:** M/M
- **Data-hora origem:** 22/06/26 11:05
- **Data-hora início:** 22/06/26 15:35
- **Data-hora conclusão:** 22/06/26 15:47
- **Dependências:** -
- **REQ/ADR/DT:** REV-001-A02, ADR-001
- **Observações:** Achado REV-001-A02. Fecha o último achado da REV-001.

## Planejamento Aprovado

Criar `docs/contexto-projeto-ai.md` seguindo o template 33 (Variante 2 — engenharia reversa, com nota de origem), conciso (alvo ≤ 200 linhas), fiel ao código, linkando o que já existe.

Seções com adaptações reais: Stack (versões reais do package.json; xlsx CDN 0.20.3; danfojs só em experimentos com override); Estrutura Real (divergências: utils/ no lugar de services/, data/ p/ JSON); Rotas (N/A — SPA sem router); Decisões Inegociáveis (ADR-001 idioma/i18n, sem backend, parsing isolado, aliases, xlsx CDN); Convenções (UI via UI_LABELS, log DEV sem PII, escapeHtml, validação de coordenadas); O Que NÃO Faz (sem backend/auth; "HubFlow" em rascunhos = futuro; é PWA; i18n não ativa); Referência (só docs existentes); Hierarquia; Estado Atual; Última Atualização.

### Critérios de aceite
- Arquivo existe, ≤ ~200 linhas, fiel ao package.json/código; linka só docs existentes; reflete ADR-001/REV-001; não contradiz o código.
- Sem mudança de código → tsc/lint/vitest verdes.

## Execução
- 15:35: Movida de pendentes para em-andamento; plano proposto.
- 15:44: Plano aprovado pelo humano ("sim").
- 15:45: Lido `package.json` (versões exatas, incluindo xlsx CDN e overrides).
- 15:46: Criado `docs/contexto-projeto-ai.md` (~120 linhas) conforme o plano.
- 15:47: Gates → `vitest` 235/235; `tsc -b` exit 0; `lint` exit 0.

## Decisões Tomadas
- **Adaptar o template à realidade em vez de seguir literalmente:** Rotas viraram "N/A — SPA sem router"; adicionado "✅ É PWA" (o template assumia "não PWA"); seção de divergências de pasta explicitada. O template é guia, não camisa de força.
- **Linkar só documentação existente:** não referenciei `requisitos/`/`dominios/`/`design/` (inexistentes) para não criar links quebrados; registrei explicitamente que não existem.
- **Marcar `docs/rascunhos/` como visão futura:** reforça a resolução do REV-001-A01 (evita que o "HubFlow" seja lido como spec atual).

## O Que NÃO Foi Feito (e Por Quê)
- **Não criei `requisitos/`, `dominios/`, `design/`:** fora do escopo e sem necessidade atual (o template 33 só os lista como links opcionais). Criar quando houver demanda real.
- **Não alterei o README:** ele é complementar (foco em humanos/como rodar) e já está fiel; o contexto-projeto-ai foca em "como decidir".

## Testes
- 1º (baseline) `vitest`: 235/235.
- Último `vitest`: 235/235. `tsc -b`: exit 0. `lint`: exit 0.
- Sem testes novos: mudança é doc-only (markdown); gates rodados apenas para confirmar que nada quebrou.

## Revisão
Auto-revisão (Standard): documento fiel ao package.json e ao código (verdade primária); conciso e dentro do alvo de tamanho; links só para o que existe; consolida ADR-001 e o estado pós-REV-001; corrige suposições do template (PWA, rotas). OK.

## Tarefas Geradas pela Revisão
- Nenhuma.

## Requisitos Gerados pela Revisão
- Nenhum.

## ADRs Geradas pela Revisão
- Nenhuma. (Referencia a ADR-001 existente.)

## Aprendizados Para o Projeto
- O `contexto-projeto-ai.md` é o lugar certo para consolidar, num só ponto de entrada, o que ficou espalhado por ADR + REV + tarefas: stack real, divergências de estrutura, decisões e limites (o que o projeto NÃO faz).
- Templates do pacote devem ser adaptados à realidade do projeto (ex.: SPA sem rotas, PWA) — seguir literalmente geraria documentação que contradiz o código.
