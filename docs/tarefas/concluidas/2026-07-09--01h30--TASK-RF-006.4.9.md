# TASK-RF-006.4.9 - Meu roteiro: edição de parada DESAGRUPADA + toque no mapa não faz mais toggle

- **Status:** Concluído
- **Modo:** Standard (epic .4.6, fatia — plano aprovado)
- **Valor:** Importante
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** M/G
- **Data-hora origem:** 09/07/26 01:25
- **Data-hora início:** 09/07/26 01:30
- **Data-hora conclusão:** 09/07/26 01:35
- **Dependências:** RF-006.4.8 ✅
- **REQ/ADR/DT:** fluxo §4/§8/§9; Q2 do humano (membros por raio + lista ±, sem toggle no mapa)

## Planejamento Aprovado

- **Edição desagrupada**: ao entrar em "Editar parada" (REOPEN), o mapa mostra os **membros como círculos** (ring + ordinal) + **candidatos pontilhados** + o círculo do raio — não mais o quadrado. Automático a partir de `draft.stopId` no `computeRoteiroMarkerModels`.
- **Toque no mapa não faz mais toggle** (Q2): `handleModelTap` durante um draft vira **no-op**; membros mudam só por raio + lista ±.

## Execução

- 01:30: Ciclo aberto.
- 01:33: `computeRoteiroMarkerModels` — durante um draft, `draftStopId = draft.stopId` PULA o quadrado da parada reaberta; `circleSource` passa a ser `points` menos os pontos de OUTRAS paradas firmadas (`otherCommittedIds`), incluindo assim os pontos da parada em edição no loop de círculos (membros = ring+ordinal; candidatos do raio = dashed). Sem draft, o caminho anterior (`unassignedPoints`) é mantido.
- 01:34: MapPage — a branch `if (draft) TOGGLE_DRAFT_POINT` do `handleModelTap` virou `if (draft) return;` (toque inerte na edição). Label `ROTEIRO_DRAFT.TAP_HINT` reescrita ("Ajuste o raio ou use os botões +/− da lista…" — a antiga "tocar no mapa adiciona/remove" ficou falsa).
- 01:35: Testes — roteiroModels (REOPEN desagrupa: sem quadrado, membros com ordinal, candidato dashed); MapPage (Editar → `data-models-summary` = "address*,address*,address" sem "stop"; toque no mapa durante a edição não muda o remaining). Aliviado o teste pesado "o círculo do preview cresce" (era 20 cliques → estourava 5s sob carga do sandbox; o clamp já é coberto por RoteiroDraftSection.test) para "segue o stepper" (poucos cliques).

## Decisões Tomadas

- **Auto-desagrupar a partir de `draft.stopId`** (sem estado novo no MapPage) — a edição sempre mostra os pontos, coerente com "editar = ver os endereços".
- **`circleSource` = pontos fora de OUTRAS paradas** durante o draft — resolve o efeito colateral do REOPEN (a parada reaberta continua em `stops`; seus pontos ficariam fora de `unassignedPoints`). Pontos tirados do draft mas ainda no raio reaparecem como candidatos (via `draftCandidateIds`).
- **Toque no mapa = no-op na edição** (não "focar/detalhe") — mínimo correto para o pedido (sem toggle); o detalhe do membro ao tocar fica para depois se necessário.
- **Teste de stress aliviado** (não mascara bug) — o clamp do raio é testado em RoteiroDraftSection.test (botão desabilitado em RADIUS_MAX); no MapPage basta o círculo seguir o stepper. Reduz renders e a flakiness de timeout sob carga.

## Testes (gates rotulados)

- `npx tsc --noEmit`: **APROVADO**
- Suíte completa `npx vitest run`: **APROVADO** — 63 arquivos, **613/613** (611 + 2 líquidos; run limpo). Nota: sob carga total do sandbox houve 2 timeouts intermitentes (o teste pesado de 20 cliques e o flake conhecido do `useRouteUploader` >10MB) — verdes em isolado e no run final após aliviar o teste pesado.
- `npm run lint`: **APROVADO**
- `npm run build`: **APROVADO**
- ⚠️ **Smoke manual (humano):** (1) "Editar parada" → o mapa **desagrupa** (círculos dos membros numerados + candidatos pontilhados + círculo do raio), **sem** o quadrado; (2) durante a edição, **tocar no mapa não adiciona/remove** nada — mexer nos membros só pelo **raio** e pelos **botões +/− da lista**; (3) Salvar reagrupa; (4) o texto de ajuda do rascunho reflete raio + lista (não fala mais em tocar no mapa).

## O Que NÃO Foi Feito (fatia final do epic)

- **Mudança do ORIGINAL** (1 clique mantém agrupado / 2 cliques expande / clicar-fora mantém foco) — a mais delicada (RF-023 travado), por último.
- Detalhe do membro ao tocar durante a edição (opcional).

## Resultado

A edição de parada agora **desagrupa no mapa** (membros como círculos + raio + candidatos pontilhados) e o **toque no mapa deixou de adicionar/remover** — a composição da parada é feita pelo raio e pela lista ± do painel, como o humano pediu. Resta só a mudança de interação do modo Original.
