# TASK-RF-006.4.12 - Altura do painel fit-content (colapsado sem corte, meio sem sobra)

- **Status:** Concluído
- **Modo:** Standard (feedback do humano 09/07)
- **Valor:** Importante
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** M/M
- **Data-hora origem:** 09/07/26 02:40
- **Data-hora início:** 09/07/26 02:45
- **Data-hora conclusão:** 09/07/26 02:55
- **Dependências:** RF-006.4.11 ✅

## Planejamento Aprovado

Colapsado cortava os botões (cabeçalho do roteiro > 224px); meio (45%) sobrava espaço. Fix: **snap colapsado = altura MEDIDA do cabeçalho** (fit-content); os fluxos do roteiro deixam de forçar "half" na seleção (ficam no colapsado-fit).

## Execução

- 02:45: Ciclo aberto.
- 02:50: `MapPanel` — grabber + `header` embrulhados num ref; `ResizeObserver` mede a altura e vira o snap **colapsado** (`collapsedPx`), com clamp `[COLLAPSED_MIN_PX=132, 70% viewport]`; guard: sem `ResizeObserver` (jsdom) ou altura 0 (pré-layout) → mantém `PANEL_COLLAPSED_PX=224` (a suíte que checa `["224px",0.45,0.9]` segue verde). `snapPoints`/`activeSnapPoint` passam a usar o px dinâmico.
- 02:52: MapPage — removido o auto-`setPanelSnap(collapsed→half)` na seleção de parada firmada, na seleção de órfão e no `handleCreateStop` (o colapsado-fit já mostra o resumo inteiro; sem sobra). `handleRoteiroCardTap` mantém half/full para o detalhe no corpo.

## Decisões Tomadas

- **Colapsado = fit do cabeçalho** (medido) — mostra exatamente a seção ativa (resumo da parada, órfão, início) sem corte nem sobra; cresce/encolhe conforme o conteúdo muda (o ResizeObserver reobserva).
- **Sem auto-raise na seleção** — antes subia p/ half (criando a sobra); agora fica no colapsado-fit.
- **Fallback 224px sem ResizeObserver** — jsdom/tests não medem; mantém o valor calibrado (contrato do MapPanel.test intacto).
- **`PANEL_COLLAPSED_PX` (224) segue como padding do fitBounds** — aproximação boa o suficiente; o painel do roteiro um pouco mais alto pode deixar marcadores um tico atrás, aceitável (anotado).
- Card-detail no corpo ainda usa half/full — sobra residual possível em detalhe curto; **anotado p/ smoke** (secundário; a queixa principal era o resumo).

## Testes (gates rotulados)

- `npx tsc --noEmit`: **APROVADO**
- Suíte completa `npx vitest run`: **APROVADO** — 63 arquivos, **620/620** (1 falha intermitente = flake conhecido do `useRouteUploader` >10MB sob carga; **18/18 verde isolado**).
- `npm run lint`: **APROVADO**
- `npm run build`: **APROVADO**
- ⚠️ **Smoke manual (humano):** (1) no Meu roteiro, selecionar uma parada → o painel colapsado mostra o **Resumo + Editar/Desfazer + Endereço** SEM cortar os botões e SEM espaço vazio; (2) órfão selecionado e fluxo de início também fitam (mais curtos); (3) arrastar p/ cheio → a lista; (4) calibrar no device o piso (132px) e conferir que o painel **cresce/encolhe suave** ao trocar de contexto (ResizeObserver); (5) card do endereço (detalhe) ao expandir — ver se ainda sobra espaço no half (se sim, refino numa próxima).

## Resultado

O painel do Meu roteiro passa a ter altura fit-content no colapsado — mostra exatamente a seção ativa, sem cortar botões nem deixar sobra. Falta o destaque de seleção (TASK-RF-006.4.13).
