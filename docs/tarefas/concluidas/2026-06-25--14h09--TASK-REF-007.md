# TASK-REF-007 - Remover inferência de área de risco / ESEDC (Correios) por completo

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** IMEDIATA (puxada no lote da rota única, 25/06)
- **Esforço-H/IA:** M/M
- **Data-hora origem:** 22/06/26 23:55
- **Data-hora início:** 25/06/26 13:51
- **Data-hora conclusão:** 25/06/26 14:09
- **Dependências:** ADR-005
- **REQ/ADR/DT:** ADR-005; RF-17 (🚫), RN-08

## Planejamento Aprovado

Remover o ESEDC ("o Correios entrega neste CEP?") **por completo** — código, dados, UI, labels, ícones e tipos (ADR-005). Decisão de produto: o app é "roteirizador a pé"; a entrega domiciliar dos Correios não é sinal do domínio e a base de CEPs é grande/desatualizável. Feito **primeiro** no lote porque enxuga a superfície que RF-015/017/018 também tocam (Sumário, popup, tabela).

## Execução

**Deleções (dados + módulo + teste + assets):**
- `src/utils/correiosDelivery.ts` (lookup CEP→entrega)
- `src/data/zipcodes-delivery-status-correios.json` (base de CEPs)
- `src/__tests__/utils/correiosDelivery.test.ts`
- `src/assets/icons/markers-map/with-correios/` (10 PNGs: home/office/indefinite × entrega/sem-entrega × processado)

**Código (dereferência completa):**
- `constants/index.ts`: removidos `DELIVERY_KEYS` e os **10** `ICON_KEYS` de entrega (`*_WITH_DELIVERY`, `*_WITHOUT_DELIVERY`, `*_CORRECTED`). Sobram HOME/OFFICE/HOME_CORRECTED/OFFICE_CORRECTED/INDEFINITE. `IconKey` (derivado) encolheu sozinho.
- `constants/uiLabels.ts`: removidos `ROUTE_MAP.TOOLTIP.CORREIOS`, `ROUTE_SUMMARY.CORREIOS_NO_ENTRY(+TOOLTIP)`, `ROUTE_SIMPLE_TABLE.CORREIOS_DELIVERY(+TOOLTIP)`.
- `utils/iconPicker.ts`: `pickIconKey(classification, original)` — removido o 3º parâmetro `delivery` e todos os ramos de entrega.
- `utils/mapIcons.ts`: removidos os 14 imports `with-correios` e as 10 entradas do registro `getIcons`.
- `utils/formatters.ts`: removidos `countRestrictedZipcodes`, `formatDeliveryLabel` e os imports órfãos (`getCorreiosDeliveryStatus`, `DELIVERY_KEYS`, `UI_LABELS`).
- `hooks/useRouteSummary.ts` + `types/hooks.ts`: removido `correiosDeliveryCount`.
- `types/index.ts`: removido `CorreiosEntry`.
- `components/RouteSummary.tsx`: removida a linha **ESEDC** do Sumário.
- `components/RouteMap.tsx`: removidos `getCorreiosDeliveryStatus`/`formatDeliveryLabel`, a linha **"Correios entrega aqui?"** do popup e o status interno.
- `components/RouteSimpleTable.tsx`: removida a **coluna Correios** (badge verde/vermelho, render, coluna virtual, tooltip).

**Testes:** `iconPicker`/`formatters`/`RouteMap`/`RouteSummary`/`useRouteSummary`/`RouteSimpleTable` limpos dos casos/mocks de Correios.

## Decisões Tomadas
- **Remoção total, não feature-flag:** ADR-005 já decidira o descarte; manter código morto atrás de flag seria dívida. Deletados dados (JSON), módulo, ícones e tipos.
- **Pasta `without-correios` mantida com o nome atual:** renomear (6 imports + mover binários) é churn sem ganho funcional; fica como cosmético futuro se incomodar.

## O Que NÃO Foi Feito (e Por Quê)
- **Renomear assets `without-correios`** → cosmético, fora do escopo (ver acima).
- **`DELIVERY_TIME` / "Delivery Time" / `deliveryTime`** → **mantidos**: é a coluna **tempo de entrega**, não tem relação com ESEDC/Correios.

## Testes
- `npx tsc -b`: **APROVADO** — exit 0.
- `npx vitest run`: **APROVADO** — **251/251** (25 arquivos).
- `npm run lint`: **APROVADO** — exit 0.
- `npm run build`: **APROVADO** — exit 0 (verifica que nenhum import de asset quebrou após deletar `with-correios`).

## Revisão
Auto-revisão (Standard): varredura final por símbolos removidos (`getCorreiosDeliveryStatus|DELIVERY_KEYS|CorreiosEntry|formatDeliveryLabel|countRestrictedZipcodes|correiosDelivery|CORREIOS_*|*_DELIVERY|risco_correios`) em `src/` → **0 ocorrências**. Build real confirma resolução de assets. OK.

## Requisitos Atualizados
- **RF-17** (ESEDC) → 🚫 **removido** (era "planejado descartar"). **RN-08** → 🚫 removido. **RF-09/11/12/15** tiveram a referência ao ESEDC retirada.

## ADRs Geradas
- Nenhuma nova. Implementa a **ADR-005** (que deve passar a "Implementada").

## Aprendizados Para o Projeto
- **"Delivery" é ambíguo no código:** `Delivery Time` (tempo) ≠ entrega dos Correios. Uma varredura cega por `delivery` teria removido a coluna de tempo. Distinguir o sinal de domínio antes de apagar.
- **tsc/vitest não pegam asset deletado:** o teste mocka `mapIcons`; só `vite build` exercita os imports reais de PNG. Build é gate necessário quando se deletam assets referenciados pelo bundler.
