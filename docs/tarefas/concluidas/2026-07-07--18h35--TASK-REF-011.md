# TASK-REF-011 - Remover o fluxo inline legado + aposentar AddressSheet/modal + hook useManifestFromUrl

- **Status:** Concluído
- **Modo:** Standard
- **Valor:** Importante
- **Urgência:** Normal
- **Esforço-H/IA:** M/M
- **Data-hora origem:** 05/07/26 21:05 (revisão da TASK-RF-022.6; escopo ampliado na RF-023.5)
- **Data-hora início:** 07/07/26 18:21
- **Data-hora conclusão:** 07/07/26 18:35
- **Dependências:** TASK-RF-022 ✅, TASK-RF-023 ✅
- **REQ/ADR/DT:** RF-07/08/12/13 (atualizados); RN-21/23 preservadas

## Planejamento Aprovado

RouteViewer → **HomePage.tsx** (só FileUploader + navegação pós-upload); órfãos deletados (RouteSelector, RouteSearchByAT, useRouteSearch, **AddressSheet**); **RouteMap só-embedded e totalmente controlado** (`interaction`/`onInteractionChange` obrigatórias; sem portal/botão fechar/Escape/sheet; caem `embedded`/`onClose`/`availableCols` morto); labels mortos removidos; hook **`useManifestFromUrl`** desduplicando Summary/MapPage; docs. Decisão: falha de salvamento deixa o usuário na HOME com o aviso (sem fallback inline).

## Execução

- 18:21: Ciclo aberto. Grep confirmou: nenhum uso dos órfãos fora do conjunto (só comentários).
- 18:22: `HomePage.tsx` novo (uploader + efeito de navegação; doc registra a decisão do save-error); `App.tsx` atualizado; `RouteViewer.tsx` deletado.
- 18:24: `RouteMap`: portal/overlay, botão "Fechar Mapa", Escape legado e AddressSheet interno removidos; props `embedded`/`onClose`/`availableCols` caem; controlado-único (o `applyInteractionRef` vira só o latest-callback do `onInteractionChange`).
- 18:26: `hooks/useManifestFromUrl.ts` novo (params + load-once + `currentRows` memoizado); SummaryPage e MapPage desduplicadas (MapPage também perdeu um efeito de load órfão pego pela suíte).
- 18:27: Deletados: RouteSelector, RouteSearchByAT, useRouteSearch, AddressSheet + 5 arquivos de teste. Labels: `ROUTE_MAP.CLOSE` e `ADDRESS_SHEET.{ARIA,CLOSE,STOP,SEQUENCE,ADDRESS,NEIGHBORHOOD,ZIPCODE}` removidos (ficam os do painel: PACKAGES_HEADER/COMPLEMENT/NO_COMPLEMENT/TYPE/TYPE_LABELS/GOOGLE_MAPS).
- 18:28–18:33: Testes: `RouteMap.test` **reescrito** (controlado-único: init/config, marcadores/bounds com `bottomObstructionPx`, transições emitidas — clique em parada/mapa vazio, refit por interação, robustez, cleanup); `HomePage.test` novo (5: uploader-only + 3 navegações + save-error fica); `useManifestFromUrl.test` novo (3: load-once, URL malformada, rota inexistente); MapPage.test com stub sem `embedded`; integração ajustada.
- 18:34: Docs: RF-07 (chips da aba Rotas), RF-08 (filtro da aba), RF-12 (AddressSheet aposentado), RF-13 (tela `/mapa`); design doc §2 (nota final do AddressSheet).

## Decisões Tomadas

- **RouteMap controlado-único**: o fallback interno existia só para o modal legado — sem ele, prop obrigatória elimina um modo de uso morto.
- **Save-error sem fallback**: IndexedDB falhou → usuário fica na HOME com o aviso do FileUploader e re-tenta (testado).
- `availableCols` do RouteMap era prop morta (nunca lida) — removida junto.

## Testes (gates rotulados)

- `npx tsc --noEmit`: **APROVADO**
- Suíte completa `npx vitest run`: **APROVADO** — 48 arquivos, **443/443** (antes: 502; **remoção líquida de ~59 testes legados**, esperada e compensada por 8 novos + rework do RouteMap.test)
- `npm run lint`: **APROVADO** (após `--fix` de formatação)
- `npm run build`: **APROVADO** (bundle −6 KB; 3 módulos a menos)
- ⚠️ **Smoke manual (humano):** HOME sobe romaneio e navega (single → Sumário; multi → Rotas; duplicado → card destacado); Sumário/mapa seguem funcionando ponta a ponta (nada visual mudou nessas telas).

## Resultado

Código morto zerado: HOME é só upload; o mapa tem UM modo (embedded controlado); o detalhe do endereço tem UMA casa (painel); as telas de foco compartilham UM hook de carregamento. Base limpa para o RF-006/010 (Meu roteiro).
