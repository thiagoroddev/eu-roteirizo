# TASK-RF-006.4.7 - Painel da parada firmada: layout (Ver lista topo-dir + Editar/Desfazer rodapé) + âncora do veículo

- **Status:** Concluído
- **Modo:** Standard (feedback do humano 09/07 sobre o painel do Meu roteiro)
- **Valor:** Importante
- **Urgência:** IMEDIATA
- **Esforço-H/IA:** M/M
- **Data-hora origem:** 09/07/26 00:10
- **Data-hora início:** 09/07/26 00:15
- **Data-hora conclusão:** 09/07/26 00:40
- **Dependências:** RF-006.4.6 ✅
- **REQ/ADR/DT:** fluxo §2/§9 (parada do veículo/âncora); gerou TASK-RF-006.9 (geocoding, pendentes)

## Planejamento Aprovado

1. **RoteiroStopSection**: "Ver lista completa" no topo-direito (mesmo lugar do Original) + "Editar parada"/"Desfazer parada" no rodapé da seção, centralizados.
2. **"Ver lista completa" (Meu roteiro)** = lista completa dos endereços da parada no painel (reusa `StopItemList` + `panelView` "list"); **só no painel** por ora — desagrupar no mapa fica na fatia de interação (.4.8).
3. **"Endereço selecionado" (Meu roteiro)**: label "Endereço selecionado — parada do veículo (âncora)"; no lugar do "1º" → ícone do veículo (`Car`) + endereço **sem complemento** (placeholder = 1º endereço da parada).
4. Registrar TASK em `pendentes.md`: geocoding mínimo do endereço da parada do veículo (depois).

## Execução

- 00:15: Ciclo aberto.
- 00:20: `StopItemRow` ganhou prop `markerGlyph?: "vehicle"` — renderiza o glifo `Car` (lucide) no mini-marcador, na cor de infraestrutura (`ROTEIRO_MARKER_COLORS.vehicle`), no lugar do número. Label `SECTION_SELECTED_ANCHOR`.
- 00:28: `RoteiroStopSection` reescrito: "Ver lista completa"/"Esconder lista" no slot `actions` (topo-direito) + Editar/Desfazer centralizados no rodapé do "Resumo da parada"; seção "Endereço selecionado — parada do veículo (âncora)" com `markerGlyph="vehicle"`, some quando a lista está aberta (a lista É os endereços).
- 00:34: MapPage — `stopAnchorItem` (1º endereço, complemento forçado a NO_COMPLEMENT), `stopListItems` (todos os membros por ordinal); reusa `panelView`/`handleShowList`/`handleHideList`; body do stop-selected renderiza `StopItemList` (neon) quando `panelView === "list"`; `panelView` reseta p/ "selected" ao selecionar/limpar parada (handleModelTap stop / handleCreateStop / handleMapTap / handleEdit/Dissolve). Efeito da decisão C inerte já anotado (.4.6).
- 00:38: Testes — RoteiroStopSection.test reescrito (âncora sem "1º", layout Ver-lista/Editar/Desfazer, lista aberta esconde âncora); MapPage.test: tap-na-parada revisado ("1º" → SECTION_SELECTED_ANCHOR + glifo) + novo teste da "Ver lista completa"/"Esconder lista". Registrada TASK-RF-006.9 (geocoding) em pendentes.

## Decisões Tomadas

- **"Ver lista completa" só no painel nesta fatia** — o desagrupar do MAPA (duplo-clique / esta lista) fica na fatia de interação (.4.8), como o humano separou ("depois"). Reusa o `panelView` do Original (mesma máquina de estados, Escape/arrasto já tratam).
- **Âncora = 1º endereço como placeholder** — a "parada do veículo" é um ponto na rua sem endereço próprio; geocoding real vira TASK-RF-006.9. O glifo do veículo + label deixam claro que é a âncora, não uma entrega.
- **Editar/Desfazer no rodapé centralizados** (não no slot actions) — abre o topo-direito para o "Ver lista completa" na mesma posição do Original.

## Testes (gates rotulados)

- `npx tsc --noEmit`: **APROVADO**
- Suíte completa `npx vitest run`: **APROVADO** — 63 arquivos, **608/608** (606 + 2 líquidos; RoteiroStopSection + tap-na-parada revisados por definição).
- `npm run lint`: **APROVADO** (warning pré-existente de inline-style no mini-marcador — paleta funcional por design, não é erro).
- `npm run build`: **APROVADO**
- ⚠️ **Smoke manual (humano):** (1) tocar uma parada firmada → "Resumo da parada" com **"Ver lista completa" no topo-direito** e **Editar/Desfazer centralizados embaixo**; (2) seção **"Endereço selecionado — parada do veículo (âncora)"** com **ícone de carro** (não "1º") + endereço **sem complemento**; (3) "Ver lista completa" → lista dos endereços por ordinal (1º/2º…), rolável no snap full; "Esconder lista" volta ao resumo; (4) o **mapa continua agrupado** ao ver a lista (o desagrupar no mapa é a .4.8); (5) Editar/Desfazer seguem funcionando.

## O Que NÃO Foi Feito (fatias seguintes do epic)

- **.4.8** — desagrupar no MAPA (duplo-clique / "Ver lista completa" também expande os marcadores) + editar sem toggle no mapa + modelo 1-vs-2-cliques nos dois modos (inclui o Original manter agrupado no 1º clique). "Ver lista completa" passará a também expandir o mapa quando essa fatia entrar.
- **Geocoding** da âncora → TASK-RF-006.9 (pendentes).

## Resultado

O painel da parada firmada no Meu roteiro ganhou o "Ver lista completa" no lugar do Original (topo-direito) com Editar/Desfazer centralizados embaixo, e a seção "Endereço selecionado" virou a **parada do veículo (âncora)** — ícone de carro + endereço sem complemento (placeholder até o geocoding). A lista completa abre no painel (rolável). O desagrupar no mapa e o modelo de 1-vs-2-cliques (com o Original) seguem na próxima fatia.
